# QR-05Q protocol: unchanged-summary three-layer portability

6 September 2026. Prospective bounded investigative gate, fixed before expanded
domain enumeration or closure search. Research base: pushed QR-05P commit
`493a9bf1014ceaa687dd3934a0cc9ddf55e50783`.

## Question and scope

Does the unchanged H=(frame,B,M,T) summary remain strongly closed under
the unchanged two-color independent thinning law after the prescribed
append-only three-layer expansion? Success and failure are both admissible
investigative outcomes. On failure retain an exact canonical obstruction;
do not repair features, compute a stable refinement, or publish a global
quotient. P's finite minimality result is not assumed to transfer.

This isolated Python/Markdown study does not import RET, core, mutable
Track-B sources, or prior executors at runtime. No metric, spacetime,
quantum dynamics, continuum, ontology or physical observation is inferred.
The orders are supplied finite mathematical models. This is not an empirical
test, a new physical law, or a Lean proof.

## Domain, fixed before search

Regenerate the original N/O family, with identical profile order and masks:

0 ferrers6: i+1<j+4 iff i<=j, i,j=0..2.
1 standard_example3: i+1<j+4 iff i!=j.
2 chain6: all increasing interior comparisons.
3 ferrers6_fixed: source0 with fixed interior vertex3.
4 path6: 1<2,1<4,3<4,3<6,5<6.
5 cycle4_edge: 1<4,1<6,3<4,3<6,2<5.
Profiles6+h (h=0..511), layered_oe_000..511, use bit3i+j for
odd2i+1 < even2j+2. Profiles518+h, layered_eo_000..511,
reverse those same incidence bits, without a transpose.

Append profiles1030+h, three_layer_forward_000..255, and
profiles1286+h, three_layer_reverse_000..255. The eight possible generating
edges in bit order are:
(1,3),(1,4),(2,3),(2,4),(3,5),(3,6),(4,5),(4,6).
Reverse each eligible arrow for the reverse source with the same mask.
There are no independently chosen skip-layer edges. Compute the full strict
transitive closure of each full source before inducing observations.
In both orientations,0 is below every interior vertex and7 above every
interior vertex; probes are never reversed.

Frames are exactly
{frame_id:0,density:"12",fixed:[0,7],eligible:[1,2,3,4,5,6]},
{frame_id:1,density:"12",fixed:[0,3,7],eligible:[1,2,4,5,6]}.
Only profile3 has frame1. All new profiles use frame0. Named colors remain
original-ID odd/even parity, not layer membership. Local indices need not
be topological. Density is an inert retained frame mark, not an inferred
physical density.

Enumerate ascending profile then ascending mask over ascending eligible IDs.
Kept IDs and local predecessor rows are sorted. Deduplicate exact
[frame_id,kept,past] by first occurrence. Each profile's state_ids list is
indexed by mask; this records every alias exactly once. Aliases are provenance,
never probability weights. No redundant per-state alias list is needed.
Retain the old1,030 profiles,65,888 aliases and2,470 raw states as an exact
prefix/restriction, including old frame and feature values.

Prospective hard caps deliberately use simple conservative bounds:
1,542 profiles;98,656 aliases;35,238 states (=2470+512*64);
472,428 fine atoms (=99180+512*3^6). Deduplication can only reduce these.
Integers/Fraction components are bounded to4,096 bits; native JSON only.
Working output cap512MiB, final artifact cap64MiB. A cap breach is an
explicit unsuccessful execution, never truncation or silent domain reduction.

## Unchanged calculus and observables

Each currently eligible odd vertex survives independently at x, and even
at y. Fixed vertices survive certainly. Retaining o,e from O,E has law
x^o(1-x)^(O-o)y^e(1-y)^(E-e), represented by an integer4x4 coefficient
table with x-power outermost. All structural atoms remain even at rate zeros.
Rows normalize coefficientwise and all four corners are deterministic.

N_q counts endpoint chains0 to7 with q internal vertices, q=0..3.
D_qoe counts chains by eligible odd/even support.
B_qroe counts ordered chain pairs by eligible union sizes.
Self-pairs and both pair orders count; a fixed interior3 is not eligible.
D is4x4x4, B is4x4x4x4, B_0r=D_r, N_q=sum D_qoe.

M counts eligible induced uniformly color-oriented K2,2 four-sets.
T counts eligible induced uniformly color-oriented P4 four-sets.
Both require two odd/two even vertices and no within-color comparison.
M requires all four cross comparisons with one uniform orientation;
T requires exactly three with one uniform orientation and the fourth
incomparable in both directions. Count both orientations, each support once.
Use the full induced comparability relation, not cover edges or directed
three-step paths. These M/T supports are disjoint and M+T<=9.
Their conditional expectations remain x²y² times their current counts;
verify both identities coefficientwise on every state.

H key is [frame_value,B,M,T], frame_value=[density,fixed,eligible].
Assign H class IDs by first occurrence. Push every complete fine row into
H classes; compare all members with their class representative, coefficientwise.
First failure means ascending class ID, then first unequal member in its
ascending member list, then first unequal target class in the union of row
supports. Missing structural target has the zero table. Retain that witness.
The first distinguishing rate is the first of the16 nodes
{0,1/3,2/3,1}² in lexicographic product order. Bidegree<=3 makes this grid
an exact interpolation cross-check, not an unbounded sampled approximation.

Audit all fine probabilities at those16 points followed by
(1/2,1/2),(2/5,3/7),(1/5,4/5),(1/2,1/3),(0,2/5),(3/7,1).
Use exact Fractions. If H closes, build its representative quotient and
verify Q(x,y)Q(a,b)=Q(xa,yb) in all256 four-variable coefficient cells for
every structural start/end pair. Fresh independent predetermined stages
are the premise. If H fails, quotient_rows=[] and semigroup=null.

## Independent routes and output contract

Both public analyze(problem) accept exactly
{schema_version:"det8-qr05q-problem-v1",family:"qr05q_three_layer"}.
Reject any other fields, values or non-native JSON types with ValueError.
No input payload supplies a precomputed outcome, feature, alias or source.

The primary generates sources and closes them transitively, recognizes
observed chain subsets/ordered support unions and quartet features, and
uses binomial coefficients for fine laws. The independent reference uses
its own source generation/reachability, directed chain traversal, full
induced count-law moments for B, alternative motif recognition, and exact
rational interpolation of direct-product probabilities for fine laws.
Static local copies of each route's own prior lineage utilities are allowed;
runtime imports/reads of other executors or prior artifacts are not.
The runner independently reconstructs the raw domain, features and laws.
Agreement alone is not the only check. JSON-only independent postflight
reconstructs the retained certificate without importing executors or runner.

analysis has exactly
{domain,features,partition,fine_kernel,pushed_rows,closure,
 quotient_rows,semigroup,expected_updates,counts}.

domain={frames,profiles,states}. profiles are
{profile_index,name,frame_id,past,state_ids}, exactly the old profile schema.
states are {state_id,frame_id,kept,past}.
features are state-ordered
{state_id,color_sizes,chain_counts,mean_graded,pair_graded,
 motif_supports,motif_count,path_supports,path_count}.
partition={state_classes,classes:[{class_id,key,members}]}.
pushed_rows=[{state_id,transitions:[{target_class,coefficients}]}],
with ascending target IDs and all structural targets.

Fine rows are internally
[{state_id,transitions:[{target_state,coefficients}]}], sorted by target.
fine_kernel={sha256,transition_atoms,coefficient_cells,normalization_rows,
 deterministic_corner_rows,rate_points,evaluated_atoms}.
The digest covers all canonical fine rows; rate_points is the integer22.
normalization_rows is the state count; deterministic_corner_rows is4 times
the state count; evaluated_atoms is22 times fine atoms.

closure={closed,classes_checked,member_comparisons,witness}.
All classes and all nonrepresentative members are checked even after a failure.
member_comparisons=sum(|class|-1).
witness=null on success; otherwise
{class_id,left_state,right_state,target_class,left_coefficients,
 right_coefficients,first_distinguishing_rates}, rates two canonical strings.
quotient_rows=[{class_id,representative_state,transitions:
 [{target_class,coefficients}]}].
Successful semigroup={verified,rows,totals}; rows are
{class_id,transition_pairs,intermediate_paths,coefficient_cells,max_abs_residual};
totals={transition_pairs,intermediate_paths,coefficient_cells,max_abs_residual}.
No semigroup certificate is claimed on a failed expanded partition.

expected_updates={motif_cells,path_cells,max_abs_residual};
each cell count is16 times state count.
counts={profiles,aliases,states,classes,fine_atoms,pushed_atoms}.
Canonical JSON uses sorted keys, compact separators, ASCII escaping and one
final newline; SHA256 always covers those bytes. All coefficient tables
must be native integer4x4 arrays. No bool/int or tuple/list coercions.

The runner's retained suite records the identical analysis once, exact-route
agreement, independent reconstruction audit, prespecified controls, prior
bridge and totals. Its wrapper retains source/prior byte ledgers and runtime.
No external artifact is an executor input. After reconstruction, the runner
compares exact old profile/state/features/H/fine/quotient restrictions with
pinned O/P. Additional new aliases or members must never be mistaken for
changes in the old restricted family. The old quotient remains separately
valid only on its old domain, regardless of expanded closure.

## Prospective controls and lifecycle

Controls include all single-bit incidences in both blocks/orientations;
mask17's1<3<5 and retained1<5 after removing3; same-color odd grade(3,0);
full-mask N=[1,6,12,8]; endpoints under reversal; independent skip-edge
absence; fixed3 exclusion; original color identity; source/mask alias
multiplicity; fine subset closure; native-wire rejection; malformed captures;
tampered coefficients, class membership, witness and omitted structural atoms.
M/T support heredity is checked after induced deletion; observations are
not reconstructed from surviving generating edges.

Run focused and full tests in normal and optimized isolated Python with
fresh external bytecode caches. Freeze README and all source/test files,
then create results.json exclusively (never overwrite). Replay it exactly,
read-only in normal and optimized fresh processes. Check source and21 prior
artifact byte identities before and after each retained run. P artifact is
3,882,409 bytes, SHA256
c43435c4916e5c15efb373ff70795ed733d7c4e360ebec4e9a1b32f4e050b048;
P's20 priors are retained with their pinned identities.

Write RESULTS.md and roadmap outcome outside the frozen source ledger.
Only scoped Q files and roadmap may be committed/pushed. Preserve unrelated
dirty RET/core/Track-B files and all earlier frozen evidence byte-for-byte.
Any protocol/implementation correction after exploratory execution is
disclosed; no silently changed gate criterion or favorable-only reporting.
