# Renewal-port bounded verification results

12 September 2026. **The one authorized finite certificate passed.** The
[candidate](../t8-q-renewal-port-candidate-2026-09-12/CANDIDATE.md) and all
six captured sources remain unchanged. The candidate's original
not-certificate-run statement describes its earlier construction; this
separate report records the subsequent verification.

## Executed scope

The two independent routes agree on all 260 retained rows: all 259 positive
histories of lengths zero through three, plus the one named four-birth
diamond. Each row includes the full marked order and 81 matrix-unit images.
Each route also checks all 397 linear-extension replays (including the
original orders) and 260 fixed birth-label transports.

Across the base rows, all 2,340 next-branch records are retained: 1,560
positive branches and 780 individually labelled zero maps. No zero branch
is normalized or committed. The diamond's next menu is checked without
committing a fifth event. The exhaustive history census is not extended.

The exact fork/join payload collision, true permutation inverses,
same-port noncommutation, false global-stage setting control, precursor-only
outcome-read locality, complex-kernel mass witnesses and rank-one effect
identity all pass their fixed checks. These are the controls specified in
[README.md](README.md), not additional study sweeps.

## Execution and evidence

Python 3.11.6, standard-library exact fractions only. From this directory:

| Command | Result |
| --- | --- |
| `../../../.venv/bin/python study.py --tests` | 21 passed before capture; 0.398 s unittest time |
| `../../../.venv/bin/python -O study.py --tests` | 21 passed before capture; 0.414 s unittest time |
| `../../../.venv/bin/python study.py --capture` | One create-only source freeze and result capture |
| `../../../.venv/bin/python study.py --verify` | Complete source-authenticated report matches |
| `../../../.venv/bin/python -O study.py --verify` | Complete source-authenticated report matches |

The suite contains 11 mathematical/API tests and 10 evidence-lifecycle
tests. The sources passed Ruff and an independent read-only source review
before capture. No captured source was edited afterward. Replays are
read-only; no second capture was created.

- [source-freeze.json](source-freeze.json): 739 bytes; SHA-256
  `1abebdf57df16c91299af3a16cb659d0708228b72cc9ab06f2d09acbaa4b4686`.
- [results.json](results.json): 2,207,556 bytes; SHA-256
  `ec92c8c7a06064164bfb470e41d5da418afd8f25c450691306917c884a5266f2`.
- Unchanged candidate source SHA-256:
  `181c61af3ce94d98c902b2248f688bab92632c6df532e02e917822fca5961ccf`.

This human-readable RESULTS file is outside the six-source frozen set.
The capture embeds the entire source manifest and full report, not merely
these success statements.

## Claim boundary

This verifies a bounded implementation of the specified renewal-port law.
Its general positivity and birth-label covariance remain the conditional
analytical arguments, not claims established by an exhaustive search over
all inputs or histories. The port grammar, finite alternative algebra,
fixed weights and orthogonal record preparation remain added assumptions.

The certificate confirms rather than removes the transport limitation:
different off-diagonal pair-kernels can remain different while every
generated record probability stays independent of them. It establishes no
coherence-sensitive readout, quantum-to-order feedback, geometry, empirical
validation, new QR-05 letter, or RET/T8 implementation change.
