# QR-05BO results: richer marked-causal queries

9 September 2026 (Pacific/Honolulu). **Bounded gate complete.** The second
supplied causal question separates BN's membership-only collision while
preserving its whole-point geometry/density collision and absolute-scale
ambiguity. Two independent exact implementations and a third test oracle
agree on the complete report. All 22 tests pass normally and optimized;
both full replays and the single alternate-runtime reference audit match.
The first capture passed without any post-first-evaluation source correction.

This is a conditional mathematical information gain: two exact interval
probabilities identify the sampling polynomial within the declared span.
It is not a physical measurement, arbitrary density/metric reconstruction,
finite-shot certainty, quantum advantage or gravitational dynamics.

## What the added question recovers

The [prospective specification](README.md) retains BN's continuous density
coefficient δ∈[0,2], two geometry alternatives η∈{0,1}, and two scale
labels s∈{1,4}. It adds the supplied mark m₂=(3/4,1/2) to m₁=(1/2,1/2).
The same sampled point answers both nested-interval questions. No new
coordinate observation or clock reading is inferred from this construction.

For normalized density f=c₀+c₁uv+c₂u²v², normalization and the two
population probabilities give M c=(1,q₁,q₂). The retained certificate is

```text
M = [ 1      1/4      1/9   ]
    [ 1/4    1/64     1/576 ]
    [ 3/8    9/256    3/512 ],       det(M)=5/32768.

M⁻¹ = [  1/5       16       −128/15 ]
      [ −16/3     −704/3     512/3  ]
      [  96/5      384      −1536/5 ].
```

Both inverse identities hold exactly. A rectangle with side-product t
has moment row (t,t²/4,t³/9); three distinct positive products give the
nonzero scaled Vandermonde determinant. This algebraic argument applies
throughout the declared polynomial span, not just at the 16 validation
fixtures. It does not extend to arbitrary densities outside that span.

Unique coefficients are not yet a geometric explanation. For this family,
c₀>0 and the ratios α=c₁/c₀, β=c₂/c₀ must satisfy

```text
α=η+δ,  β=ηδ.
η=0: (α,β)=(δ,0).
η=1: (α,β)=(1+δ,δ).
```

For each η, derive δ, apply the supplied bound and certify both original
forward probabilities and normalization. Analytically, the two parameter
curves intersect only at (α,β)=(1,0): flat δ=1 versus conformal δ=0.
Consequently, within the entire declared continuous family, the paired
population law identifies the geometric alternative except at this
compensated pair when both nuisance values are admitted. Scale remains
unidentified everywhere. This is a model-relative factorization argument,
supported by the exact reconstruction certificate—not a finite-grid lookup
or a proof that nature selects this family.

### Controlled information gain and its limit

| Prespecified pair | Common first probability q₁ | Flat q₂ | Conformal q₂ | Result of added query |
|---|---|---|---|---|
| Flat δ=1 / conformal δ=0 | 17/80 | 21/64 | 21/64 | Whole-point collision remains |
| Flat δ=16/11 / conformal δ=45/158 | 1/5 | 5/16 | 2275/7296 | Paired laws differ by q₂ gap 5/7296 |

Both pairs have geometric-target gap 3/80. Across the 16 fixtures, the
complete first-query laws form six classes; paired laws form seven, exactly
matching the seven complete point-law classes. The added question splits
the second pair and its scale copies, not the first pair. Equality of the
first pair's whole point laws prevents every common observation channel
from distinguishing it under the same acquisition premises.

The gap 5/7296 is exact but small. This study does not establish that four
records reliably distinguish the two models, or quantify the needed sample
size. The inverse coefficients also show why reconstruction stability under
population uncertainty deserves a separate gate.

## Conditional target sets and continuous nuisance

The target τ=μ(I₁)/μ(Q) is 1/4 for flat geometry and 17/80 for the
conformal alternative. In the table, F denotes {1/4}, C denotes {17/80},
F,C denotes the discrete set {17/80,1/4}, and ∅ means model infeasibility.
Each feasible geometry retains both scale labels.

| Stipulated data ID | δ∈[0,0] | δ∈[0,1/2] | δ∈[0,1] | δ∈[0,2] |
|---|---|---|---|---|
| flat_0 | F | F | F | F |
| flat_1 | C | C | F,C | F,C |
| flat_2 | ∅ | ∅ | ∅ | F |
| flat_interior | ∅ | ∅ | ∅ | F |
| conformal_0 | C | C | F,C | F,C |
| conformal_1 | ∅ | ∅ | C | C |
| conformal_2 | ∅ | ∅ | ∅ | C |
| conformal_interior | ∅ | C | C | C |
| zero, one, equal, zero_constant (four separate data rows) | ∅ | ∅ | ∅ | ∅ |

All 48 cases are retained: 16 target-identified, four ambiguous and 28
infeasible. The 192 labeled hypothesis rows contain 48 feasible and 144
rejected rows. The four ambiguous case rows repeat one population pair
under two data IDs and two bounds; they are not four distinct ambiguous
laws. All 36 adjacent-bound checks preserve label/target subset
inclusion and surviving exact δ values. Singular first-query inverses,
closed-bound endpoints, rejected algebraic candidates and second-query
residuals remain visible. The continuous δ is solved, not selected from
the fixture list; the two prespecified non-fixture δ=1/3 API checks pass.

Data IDs name synthetic origins, not actual-world input available to the
solver. For example, `flat_1` has the same population pair as `conformal_0`;
under the uniform premise only the conformal explanation is admitted.
If the data really came from the nonuniform flat generator, that premise
would be false. A conditional target answer does not authenticate its
acquisition assumptions. Likewise, these case counts should not be compared
with BN's counts as a performance score: the fixed data domains differ.

The `zero_constant` control reconstructs f=4uv, with coefficient vector
(0,4,0). This is a normalized nonnegative density outside the positive-
constant factor family, not malformed probability data. The admission
guard checks c₀ before division and rejects every geometric candidate.
The other legal nested control pairs likewise produce no admitted model.
An empty set does not diagnose a particular apparatus failure or refute
unrestricted geometry.

## Same-point coupling and finite-record limits

Every world retains all 256 four-attempt paired words: 81 have positive
probability, and 175 contain the structurally impossible 10 symbol. Across
16 worlds that is 4,096 rows, including 2,800 zero rows. All 256 retained
first-query marginal rows agree with their complete Bernoulli word laws.
All probability-zero rows are exact Fractions, not omitted branches.

The one-point law is (1−q₂,q₂−q₁,0,q₁). The independent-marginal shortcut
would assign q₁(1−q₂)>0 to 10 in every admitted world; that is a different,
incompatible channel, not an approximation silently accepted by this gate.
Fresh attempts are iid; the questions within an attempt are coupled.

Positivity of the three category masses is certified throughout δ∈[0,2],
not inferred from the validation fixtures. Every possible word therefore
retains all four continuous-family geometry/scale labels and both targets
under every density bound. All-00 and all-11 packets remain possible,
although the exact population pairs (0,0) and (1,1) are model-infeasible.
Finite frequencies must not be substituted for stipulated population pairs.
Under the nested iid model, the two counts determine the three category
counts; preserving pairing for auditing is not a claim that attempt order
is statistically necessary. An impossible packet does not authorize
detector correction, record deletion or a new-physics claim.

All eight scale controls preserve paired/point laws and targets while
absolute proper volumes multiply by four in Q, I₁ and I₂. No quantum-state
or passive-chart enumeration was rerun. Added access and polynomial
interpolation are verifier constructions, not newly acquired measurements;
interpolation nodes outside Q are algebraic evaluations of the specified
polynomial, not extra observations or sampler support.

## Verification, chronology and retained evidence

The gate began from pushed BN commit
`a4b67c1494169e6f9107e971cc3b09531d4709d1`. The README and protocol
preceded implementations and fixed mathematical execution. Primary
monomial integration/elimination/q₁ inversion agrees with independent
direct-function tensor Simpson/cofactor reconstruction/factorization.
The third oracle uses endpoint antiderivatives, a permutation determinant,
rational orthogonalization and scalar word laws. Full native reports are
compared with exact types, not only by summaries or hashes.

Prefreeze query hardening checks both pairs' 31-bit integer caps before any
gcd/Fraction work. The public solver supports bounded rational pairs beyond
the fixtures, uses continuous family equations without file/engine/previous-
capture access, and keeps inputs/outputs detached. It refuses malformed,
non-nested, over-cap or coercive inputs and record/frequency-specific schemas
or extra fields. A legal nested pair outside the model returns an empty
result. No interface can detect a disguised sample frequency from its
rational value alone or authenticate a declared external density bound.

A connection interruption during authoring was resolved before freezing.
The independent mathematical test prefix was retained; root completed the
focused integration/deadline tail, and both were independently statically
reviewed. No authoring agent executed mathematical code before release.
Existing Ruff and normal/optimized syntax checks passed. A metadata-only
inspection initially attempted literal evaluation of a source-path tuple
containing a variable; narrowing it to literal protocol/hash assignments
resolved that inspection issue without changing model code or running math.

The create-only freeze was produced at approximately **21:35:11 UTC on
9 September 2026**, before the first capture. Nine identities bind six BO
specification/code/test files, BN's README/RESULTS and the pinned BM utility
driver. Only authenticated exact codecs/comparison, bounded I/O, exclusive
writes and deadline helpers are reused. Their isolated globals are retained;
BO owns its schemas and publication lifecycle. No prior mathematical
executor, estimator, test tree or publication wrapper was run.

| Check | Observed outcome |
|---|---|
| Python 3.14.0 first capture | Complete native primary/reference agreement; 0.344462 s |
| Normal suite | 22 passed; 0.894 s |
| Optimized suite | 22 passed; 0.900 s |
| Normal complete replay | Exact match; 0.347103 s |
| Optimized complete replay | Exact match; 0.346155 s |
| Python 3.11.6 reference-only audit | Exactly one analysis; full native and canonical match; 0.235162 s |

Each suite caches one full analysis. Focused tests cover native report
mutations, protocol/source pinning, utility identity before execution,
fresh isolated inputs, owned default paths, exclusive evidence, final
source/freeze/capture checks, canonical JSON and prior-deadline restoration.
Fault injections use mocks or temporary files, not retained evidence.
The 120-second suite alarm interrupts rather than becoming a recoverable
test error. All analysis/suite runs stayed inside the fixed 30/120-second
bounds, with no adaptive increase, numerical retry or post-first source
correction. No timing-sensitive RET rehearsal was observed at preflight.

- [Source freeze](source-freeze.json): 1,228 bytes; SHA-256
  `bf48a5e30edcb314e5a491bb67884789febb98ce9f140eece0e78645097ab434`.
- [Complete capture](results.json): 370,484 bytes; SHA-256
  `df967357d18bf384a9e1fb05a560e9b791a17382463eb2f25ba0a547e1bca105`.
- Canonical mathematical report: 369,245 bytes; SHA-256
  `f5682d5cbe91ef4a256dac7ef2962bde72caa5543735dcd9c59b4b76350f6faf`.

Publication is scoped to the nine BO artifacts and the owned roadmap.
Final checks preserve all nine frozen source identities and both retained
artifact hashes. Independent stored-evidence and scientific-prose reviews
found no outstanding issue. Documentation QA covers three scoped Markdown
files: all 84 local-link occurrences resolve, heading/fence/math delimiters
pass, and no trailing whitespace or conflict markers occur. These checks
and Ruff are static checks, not additional numerical runs.

The 231 pre-existing dirty status entries, including core/RET/application
work and the temporary model file, remain outside this change. No dependency,
physical device or external dataset was changed. These verification controls
are not a certification of a hostile operating environment.

## Next bounded question

Propose **QR-05BP: interval-valued population-pair identification**. Test
whether BO's added-query distinction survives independently supplied closed
probability tolerances. Freeze the interval boxes, density bounds and
comparison cases before execution. A probability box is intersected with
the nested-probability cone, but each geometric alternative must satisfy
both forward constraints using the **same δ**.

Positive denominators allow exact affine-inequality intersections in δ.
Retain empty sets, singleton/endpoints, whole feasible nuisance intervals
and their coupled forward-curve images—not independently chosen query
endpoints, coefficient boxes, midpoint fits, priors or target interval hulls.
Targets remain finite labeled geometric alternatives. Controls should
include point boxes recovering BO, individually feasible queries with no
common δ, nested tightening and persistent structural-zero/scale/whole-point
obstructions. Robust identification means one target across every admitted
parameter in the supplied uncertainty set.

BP has not started. Supplied population intervals are neither calibrated
error bars nor finite-shot confidence regions; constructing those requires
a separate observation/coverage contract. General-density nullspaces remain
a different limitation of the chosen span. BO does not close QR-05, validate
ontology, derive QM/gravity, or release RET applications. Book work remains
archival; Lean, physical clocks and later gravity couplings stay separate.
See the [roadmap](../../track_b/QUANTUM_RECORD_GRAVITY_BRIDGE_PLAN.md).
