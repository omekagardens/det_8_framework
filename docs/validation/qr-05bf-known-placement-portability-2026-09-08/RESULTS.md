# QR-05BF results

8 September 2026 (Pacific/Honolulu). **The bounded investigative gate is
complete.** Target-specific four-site access survives some known interior
position changes, but needs new weights. At other positions, no decoder
using those four sites can identify the target throughout the admitted
model. Restoring the fifth site and using the known position repairs
population identification in all six fixtures.

This is exact finite synthetic mathematics on supplied geometry—not an
apparatus experiment, unknown-geometry recovery, quantum advantage or
gravity law. See the [standalone model and fixed protocol](README.md),
[input](protocol.json) and [complete evidence](results.json).

## Exact criterion, not just a failed weight choice

Fresh monomial, beta and independent tensor-Boole integrals agree on

\[
q=(1/9,1/18,1/18,1/36,1/144),\qquad V=1/2,\quad\sigma=1/16.
\]

For each strictly interior z = (u,v), let A_z evaluate the four bilinear
basis functions and bubble at (00,10,01,11,cc=z), and let S omit row 11.
The full map has a two-sided inverse. The retained map has rank four and
loses exactly the coefficient direction

\[
d=A_z^{-1}e_{11}
 =\left(0,0,0,1,-\frac{1}{(1-u)(1-v)}\right)^\mathsf T.
\]

Its target image is

\[
qd=\frac1{36}-\frac{1}{144(1-u)(1-v)}.
\]

Thus four-site target recovery exists precisely on

\[
\boxed{(1-u)(1-v)=1/4}.
\]

The block-matrix argument in the protocol establishes this conditional
criterion throughout the strictly interior placement domain for this model.
The six exact calculations verify the declared fixtures; they are not a
numerical grid offered as proof of the continuous statement. Sufficiency
is supplied by the compiled row. Necessity on the admitted physical domain
is supplied by indistinguishable profile pairs with different targets
whenever qd is nonzero.

With full weights v = q A_z⁻¹, the recompiled four-site candidate drops
v_11. Its complete row residual is −v_11 e_11ᵀ, with v_11 = qd. This
candidate is a certified target decoder only on the equality locus.
Its diagnostic scores are retained elsewhere without promoting it to a
valid recovery rule. All full-weight residuals vanish.

| Position label | Known position | Frozen BE row valid? | Recompiled four-site row valid? | qd | Collision target difference |
|---|---|---|---|---:|---:|
| center | (1/2,1/2) | Yes | Yes | 0 | 0 |
| equality_u | (1/3,5/8) | No | Yes | 0 | 0 |
| equality_v | (5/8,1/3) | No | Yes | 0 | 0 |
| shift_low | (1/3,1/2) | No | No | 1/144 | 1/171 |
| shift_high | (2/3,1/2) | No | No | −1/72 | −1/99 |
| near_corner | (3/4,3/4) | No | No | −1/12 | −1/24 |

“Valid” here means the whole population target row agrees on the admitted
profile family, not that every finite sample returns the target. The
collision difference is plus-profile target minus minus-profile target.
These six cases are a fixed analytical control menu, not an estimate of
how often experimental placement errors occur.

## Updating weights versus restoring access

The full recompiled rows, in (00,10,01,11,cc) order, are:

| Position | Full target weights |
|---|---|
| center | (1/12,1/36,1/36,0,1/9) |
| equality_u | (7/90,7/180,0,0,2/15) |
| equality_v | (7/90,0,7/180,0,2/15) |
| shift_low | (5/72,5/144,1/72,1/144,1/8) |
| shift_high | (13/144,1/72,5/144,−1/72,1/8) |
| near_corner | (8/81,1/54,1/54,−1/12,16/81) |

The two moved equality cases separate stale mathematics from insufficient
access: frozen BE weights fail, while the new four-site weights work.
The coordinate-swap control permutes 10 and 01 in the geometric certificate;
it does not silently swap the asymmetric profile's fixed labels.

The other three positions have a genuine lost target direction. Recomputing
weights cannot fill missing information. Restoring 11 and applying the
full row removes target bias; A_z⁻¹ also recovers all five population
coefficients. This uses the exactly supplied position and does not infer it.
Neither inverse implies exact coefficient reconstruction from five noisy
binary outcomes.

Additional zero weights at the moved equality positions are retained in
the table. No further three-site acquisition contract or minimum-count
study was executed; BF still keeps all declared four- and five-site records.

## Admitted indistinguishability and quantum output

At each position the fixed construction uses a_− = −δd and a_+ = +δd,
where δ = 1/[2(1+|d_θ|/16)]. Their sufficient admission bounds are exactly
1/2, so the examples do not depend on invalid qubit states or an untested
interior overshoot. The scales, and full-record total variations, are
2/5, 2/5, 2/5, 8/19, 4/11 and 1/4 in protocol order.

Both members have zero response at every retained site. Their complete
four-site record laws, unnormalized branch states and both diagnostic
estimate lists coincide; retained-law total variation is zero. They have
different omitted-site states and different full outcome-averaged states.
The target difference is exactly 2δqd, including the zero equality cases.
Each pair is a profile collision; only the last three are target ambiguities.
Fresh repetition at the same retained sites cannot distinguish either
member under this stipulated response model.

State reduction must be kept separate from target recovery. All **576**
retained-outcome comparisons satisfy

\[
\rho^{(4)}_y
=\operatorname{Tr}_{11}\!\left(\sum_{x_{11}=\pm1}
\rho^{(5)}_{y,x_{11}}\right),
\]

with the full 32-dimensional sums and reduced 16-dimensional states both
retained. This holds at all six positions, including the target-ambiguous
ones, for the declared independent product preparations. However,

\[
\widehat T_{\rm full}(y,x_{11})
-\widehat T_{\rm candidate}(y)=v_{11}x_{11}.
\]

Of 1,152 full-branch pathwise comparisons, 576 differ: every branch at
each of the three nonidentifiable positions, across all six profiles.
The count includes probability-zero branches. Correctly discarding an
unobserved subsystem therefore need not preserve a specified estimator.
This is not a general entangled-state recovery or compression theorem.

## Bias and finite-shot risk remain different questions

All three estimates retain exact mean, bias, variance and MSE in the
capture. For coefficients a, the reduced bias equals the corresponding
row residual applied to a. Full-access bias is zero in all 36 cases.
The asymmetric_bubble target is 19/216 at every position:

| Position | Frozen-row bias | Recompiled-candidate bias |
|---|---:|---:|
| center | 0 | 0 |
| equality_u | 19/1296 | 0 |
| equality_v | −23/1296 | 0 |
| shift_low | 5/324 | 1/288 |
| shift_high | −7/324 | −1/144 |
| near_corner | −73/1728 | −1/24 |

An attractive finite-shot score does not certify the target row. For
example, at equality_u the biased frozen estimator has MSE 2269/139968,
below the unbiased recompiled estimator's 23107/1166400. No decoder was
selected using these scores. There is no uniform precision, allocation
optimality or calibrated hardware-cost claim. These diagonal independent
qubit likelihoods remain exactly classical Bernoulli-equivalent.

The larger continuous-profile obstruction recorded in BE is not silently
reused: its old center-blind polynomial need not vanish at a moved station.
BF neither evaluates that extension nor claims unrestricted profile
recovery. Position knowledge, apparatus calibration and model adequacy are
separate premises, not presumed explanations for inconvenient residuals.

## Verification and retained source history

The first freeze and first capture succeeded. All **37 tests passed** in
isolated Python 3.14.0 normally and with optimization enabled (12.830 s
and 12.852 s respectively). The complete independent mathematical reports
agree on six placements, 36 profile cases and **1,728** branch rows:
1,176 positive-probability rows and 552 zero rows. Both full driver replays
pass with all eleven identities and the capture unchanged.

The third test route uses verified degree-five tensor-Boole integration,
permutation-expanded cofactor inversion, rational Gram–Schmidt rank and
scalar/bit-insertion branch calculations. It compares the whole native
report, not only summary moments. These numerical methods and their
synthetic integration nodes do not add observer measurements.

Exactly one separate reference-only Python 3.11.6 audit also matches the
entire mathematical subreport: 586,266 canonical bytes, SHA256
`a497db51d227686dabdffb7a61a0bdecc5dc272b1ed3f4d42d752c765d5d6a12`.
It made one reference call (0.43319 s) with a fresh external cache and
retained unchanged protocol input, reference, all eleven source identities,
freeze and capture. No primary engine, driver, tests or historical executor
ran in that audit. This is not a full-suite Python 3.11 compatibility claim;
the recorded times are not benchmarks.

The source-bound driver statically adapts BE's lifecycle protections without
its observer: fresh verified source execution, same-byte protocol hashing
and parsing, native-type exact route/input comparisons, strict JSON and
size limits, create-only outputs, and final byte brackets. Tests exercise
malformed types, altered fields and late branches, input mutation, stale
source/freeze identities, cache bypass and publication/replay byte changes
under both modes. Failed test injections are temporary fixtures, not
alterations of the retained evidence.

The selected BE center overlap matches one placement, four base profiles,
two distinct observation plans and 192 branch rows, including complete
repair5 and target4 scores/states. It reads retained data; old engines are
not executed. BE's different collision amplitude, observer, replication
and off-model controls are not rerun or recertified here.

Before freezing, static review clarified inverse-product and locus-residual
orientation in the README, and the newly adapted sources/tests were
formatted and checked. **No post-first source, protocol, mathematical or
test correction was required.** The fixed placement menu and collision
scaling were not changed after evaluation.

| Artifact | Bytes | SHA256 |
|---|---:|---|
| source-freeze.json | 2,387 | `7417b6b3e57e3449d600f5c8b24254fb32444d6002d9e369adc7389e4098cd8a` |
| results.json | 3,101,822 | `b70e110bf7f076fa37b13ffd0f45ef1c7f339b2b16cfcd4d5219d68e176a9e30` |

Reproduce using the normal/optimized test and replay commands in the
[model sheet](README.md#source-bound-execution-and-acceptance). Do not
overwrite the retained capture. This publication starts from pushed BE
commit `428702c3a14db2d89c32b803d8078ffad6c043e6`. Separate RET/core,
application and other working-tree changes remain outside its scope.

## Next proposed gate: QR-05BG

Study **finite uncertainty about the sampling position** while keeping Q,
the target kernel, axes and profile basis fixed. The interior position is
fixed during a run but only a small prespecified menu is public. This is
not a per-shot random-position model, unknown spacetime geometry or an
empirical localization calibration.

At population level, full five-site means give the four corner coefficients
c. For each allowed position z, form

\[
\theta_z=\frac{m_{cc}-\phi(z)\cdot c}{b(z)},\qquad
T_z=q_{\rm corner}\cdot c+q_b\theta_z.
\]

Apply the declared sufficient admission bound separately to every position.
Retain the exact finite set of feasible position/coefficient/target triples,
including duplicate target values. Its minimum/maximum describe a range;
the interval hull must not be substituted for the exact discrete set.
Distinguish an empty feasible set, multiple positions with a common target,
and genuine target ambiguity.

Prespecify a small population-record menu and test admitted same-full-law/
different-target position/profile pairs, then compare disclosure of exact
position metadata. The observer must not receive the hidden position
through its labels. Full corner access already includes 11; this asks
whether missing position information creates a different ambiguity from
BF's missing-measurement ambiguity.

This is a conceptual next gate only: no BG fixture evaluation or numerical
study has run. Finite-shot confidence, priors over positions, continuous
uncertainty, apparatus data, RET integration, off-model recovery and gravity
dynamics remain outside it.
