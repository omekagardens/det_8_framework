# QR-05BL decision record

9 September 2026 (Pacific/Honolulu). **Design checkpoint complete.**
This is an analytical specification and review, not a numerical-results
capture, finite-shot inference result or apparatus-validation report.

## Selected bridge and analytical outcome

The [standalone model sheet](README.md) puts one relative spacetime volume
V(I)/V(Q) on the unknown side of a two-world inverse problem. The worlds
share causal order and three fixed marked probes but have different positive
conformal volume factors. Four fresh iid points produce only labeled causal-
membership bits. The sampling law, marked-query access and no-loss channel
are explicit premises, not derived or physically calibrated mechanisms.

Under uniform proper-volume sampling, membership probability equals the
relative volume. Direct integration gives targets 1/4 and 17/80. Their
complete population record laws differ, so the declared positive pair has
an identifiable target at law level. Every finite record word has positive
probability under both hypotheses: four observations cannot determine the
correct target with certainty. The sample mean is unbiased under the stated
sampler, not automatically under an unknown acquisition law.

The five-world expansion preserves two explicit obstructions:

- A fixed nonuniform density on the flat world exactly compensates the
  other world's geometric volume factor. The entire latent point law and
  every common observation channel agree, despite target difference 3/80.
  More samples or richer order records do not separate this pair without
  additional assumptions or independent information about acquisition.
- Multiplying the metric by four multiplies two-dimensional proper volumes
  by four while preserving normalized sampling and the target ratio.
  Identifying a relative volume does not identify absolute metric scale.

The complete-law classes are {A,D}, with target set {1/4}, and {B,C,E},
with target set {17/80,1/4}. These are population-law classes, not candidates
selected by one observed word. All five remain support-compatible with every
valid four-record word.

Four identical independent maximally mixed qubits, optionally measured in Z,
give an ordinary QM extension with no additional geometric information.
The verifier's unnormalized candidate branch matrices are mathematical
evidence, not actual-world-selected observer data. Physical conditional
payloads are normalized and identical across worlds. No quantum advantage,
new physical law, geometry-sensitive phase or gravitational coupling is used.

## Review and access corrections

Three independent read-only analytical reviews covered the geometric
identities, conventional QM extension, repository lineage and adversarial
observation controls. The specification incorporates these distinctions:

- Same continuum causal order is not, by itself, equality of a randomly
  sampled finite-order law. The density counterexample proves the stronger
  equality of the entire point measure explicitly.
- The fixed probe m is not a proper-time or volume midpoint. Marked-query
  access and causal comparison are assumptions; hidden coordinates, true
  targets, geometry-coded IDs and actual world labels are not packet fields.
- All hypothetical candidate predictions may be public model knowledge.
  An actual-world-selected numerical unnormalized branch would leak its
  probability through its trace and is therefore excluded from access.
- The count is sufficient for this fixed iid Bernoulli target. Earlier
  histogram-loss examples are not transferred to this different experiment.
- Density is defined relative to proper volume. A later passive-chart check
  must transport metric, measure, scalar density and marked regions together.
- Missing or malformed attempts cause interface refusal, not silent thinning,
  a zero membership outcome, an apparatus diagnosis or a refuted geometry.
  Loss-law examples remain analytical boundaries outside the first executable
  successor's five-world no-loss scope.

The final access correction changes only the description of information
available to the observer; it does not change the displayed quantum law.

## Execution and provenance

No numerical fixtures were evaluated, engines run, unit-test suite executed,
source/capture freeze created or apparatus records acquired in BL. No prior
engine was rerun. The fractions and law collisions are displayed analytical
identities; numerical agreement, exact report comparisons and actual test
counts belong to a separately frozen successor.

The checkpoint starts from pushed BK publication commit
`b62c7f80d0e4a636e00193ca60b44e0456c3a486`. The existing BK `results.json`
SHA-256 was read and matched
`dee443f657b267f6a082ea24f600f76fce15611eeeb0c129297df8e9c0c5b56b`.
This authenticates predecessor bytes, not inherited validation of BL's
different geometric model. D/E supply earlier observation/sampling lessons;
BB supplied geometry, and BK closed its fixed response-admission collection.
The present continuum density collision is a new analytical construction.

Documentation-only checks covered all three scoped Markdown files: all 83
local-link occurrences resolve to existing files, heading separation and
fenced/display-math delimiter checks pass, and no trailing whitespace or
conflict markers were found. Git's whitespace check also passed. These
checks concern Markdown integrity, not mathematical or physical validation.

The scoped publication contains only this decision record, its model sheet
and the Track-B research roadmap. The 231 pre-existing dirty status entries,
including separate RET/core/application work and the local temporary model
file, are outside scope. Dependencies and prior captures remain untouched.

## Next decision

Proceed next to **QR-05BM: bounded relative-volume verification**. Before
execution, freeze the five no-loss worlds, N=4, exact packet schemas, one
passive chart map, independent derivations, evidence rules and work bounds.
The prospective domain has 80 membership rows and 1,280 joint membership/Z
rows; these are planned coverage counts, not executed tests or observations.
Verify the positive correspondence and density/scale obstructions together.
BM has not been implemented or executed in BL.

This closes one geometry-identification design checkpoint. It does not close
QR-05, establish a calibrated sampler, reconstruct a general metric, derive
gravity or advance RET/application release status. Lean, clocks and later
gravity couplings remain separate. Book work remains archival. See the
[roadmap](../../track_b/QUANTUM_RECORD_GRAVITY_BRIDGE_PLAN.md).
