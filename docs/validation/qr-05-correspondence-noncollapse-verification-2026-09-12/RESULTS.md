# Correspondence/noncollapse: exact results and decision

12 September 2026 (Pacific/Honolulu). **The bounded exact verification gate
is complete.** Both independent implementations agree on every retained
comparison and closure record. All 39 tests pass in normal and optimized
modes. Three dedicated complete replays, including exactly one Python 3.11
replay, match the first nine-source-frozen capture. No actual frozen source
or fixture was repaired after mathematical execution began.

The [executable contract](README.md) implements the
[preceding analytical design](../qr-05-correspondence-noncollapse-design-2026-09-12/README.md).
The [native tagged-rational capture](results.json) retains complete evidence;
[verification.json](verification.json) records the observed command outcomes.
This is finite supplied-kernel mathematics, not an acquired spacetime,
continuum convergence result, gravitational dynamics or RET readiness.

## Verified coverage

| Retained quantity | Verified total |
| --- | ---: |
| Comparison bases / separate variant rows | 7 / 28 |
| Input node occurrences / square cells | 132 / 332 |
| Primary candidate bitmask universe | 4,752 |
| Onto-both correspondences / member occurrences | 2,380 / 12,576 |
| Full ordered raw discrepancy cells | 69,976 |
| Full ordered normalized discrepancy cells | 69,960 |
| Bijection records | 64 |
| Closure bases / separate variant rows | 4 / 16 |
| Closure nodes / square cells / comparable slots | 40 / 112 / 36 |
| Mathematical/API tests / evidence tests per mode | 22 / 17 |

The smaller normalized-cell count is deliberate: the four zero-diameter
rows return normalization unavailable. Unequal-cardinality bijections and
singleton minimum profile gaps also retain explicit unavailable/singleton
cases, not fabricated numerical values. Named variants can have identical
inputs; they are retained but are not independent statistical trials.

The primary route enumerates relation bitmasks and all strict-order paths.
The reference constructs nonempty row neighborhoods and uses a topological
max-plus recurrence. A third test oracle independently reconstructs the
complete native report using cardinality-stratified relation combinations
and permutations of path nodes, supplemented by literal analytical tables.
Full kernels, profiles, quotients, relation members, both discrepancy
matrices, all maximizing cells, minimizing relations, bijections, paths,
closure matrices, supports and residuals are checked—not just summary optima.

## Substantive finite results

The audited fork pair retains correspondence distance **1/10** while every
bijection has error at least **1**, with that bound attained. After diameter
normalization these are 1/11 and 10/11. Restricting the comparison to
bijections would therefore change the answer on this admissible directed
pair. The convention is D_C=min dis, with no extra factor of 1/2.

All retained correspondence fibers satisfy their raw and normalized
profile-distance bounds. The strict-gap sufficient condition is exercised
nonvacuously by the retained chain controls. The boundary fork with weights
1 and 3 versus the two-point chain of weight 2 has D_C=1 and both gaps 2:
equality at the threshold does not force a bijection.

The duplicated-source fork has zero correspondence distance from a chain
despite different multiplicity. The near-twin fork remains point-distinguishing
but has raw error 1/10 and normalized error 1/11. Diameter normalization
does not supply a uniform local distinction gap. The general zero-quotient,
scale-stability and ambient coverage theorems remain the design's analytical
results; this finite test does not certify unsampled coverage.

The endpoint-excess pair gives raw supremum and correspondence error 1,
versus a complete-cell mean 1/9, positive-separation-pair mean 1/3 and
endpoint-omitting diagnostic 0. No trimmed statistic is promoted to a
supremum bound. The DD paired-list control retains U=1/4, declared ℓU=1/8,
surrogate U/meanL=3/16 and missing scale factor ℓ meanL=2/3.

The fixed asymmetric scale-fit witnesses give 1/2 for a directed chain
against the half-scaled symmetric kernel, and 1 in the opposite unit-scale
comparison, with the analytical support obstruction retained in tests.
The symmetric kernel is correctly flagged invalid-causal; no general
continuous scale optimizer or physical counterexample was introduced.

All-pair closure retains endpoint 3 when cover-only closure gives 2. The
zero-cover control gains endpoint separation 1 while its first cover remains
zero: additive closure support differs from positive-weight reachability.
Zero-sum paths and all ties survive. Idempotence, extensivity and the sharp
three-chain Lipschitz witness (input difference 1, output difference 2) pass
their fixed checks. None establishes a bound uniform in growing graph size.

## Execution and evidence integrity

Before the first mathematical run, static review strengthened publication
readback, one-snapshot authentication, strict rational tags, route input
isolation and fail-fast suite limits. These were pre-freeze changes. Once
frozen, the first capture, both suites and all dedicated replays passed
without source tuning. Deliberately altered files in evidence tests are
disposable copies only; the actual sources and capture remain untouched.

| Execution | Runtime | Observed result |
| --- | --- | --- |
| Normal suite | CPython 3.14.0 | 39 tests pass; unittest 10.821 s |
| Optimized suite | CPython 3.14.0, -O | 39 tests pass; unittest 10.536 s |
| Dedicated replay 1 | CPython 3.14.0 | Full capture match |
| Dedicated replay 2 | CPython 3.14.0, -O | Full capture match |
| Dedicated replay 3 | CPython 3.11.6 | Full capture match |

The complete capped suite entry points took 11.205810 and 10.925163 seconds,
respectively, including their evidence setup/final checks. Study runs stayed
within 30 seconds and the capture is below the 16 MiB artifact cap.

```text
source-freeze.json: 1,204 bytes
SHA256 6838bd7a76cd154d79dfaa8d7aa3803af2f04a5fe5c9b3e6395974a752c875ae

canonical native report: 3,407,082 bytes
SHA256 9ba38050a8d5f20ed4ec75095759315e99719ab7c8cc232a37ae92eea572614a

results.json capture: 3,408,304 bytes
SHA256 e0f053e710fb524dada1f86dc8be4f80b1d92a47918b1e9ab8e18f12f5a399c0
```

All 55 source bindings across this gate and the five preceding evidence
bundles match. The preceding 46 bindings and five capture hashes are
unchanged; these prior checks were metadata-only, not old mathematical
replays. All six new Python sources pass static lint and Python 3.11 AST
parsing. No dependencies were installed for this verification.

## Next decision and publication boundary

**Next gate: uniform coverage/stability and noncollapse design, analytical
only.** State the ambient kernel/domain class, uniform profile-coverage and
measurement-error assumptions, scale references, and how n-dependent
max-plus stability affects any claimed convergence rate. Separate a theorem
with supplied coverage from evidence capable of certifying that coverage;
retain sampling/identifiability obstructions and the earlier DD normalization
counterexamples. Do not start a larger geometry simulation first.

Growth/null calibration, physical metrology, empirical observables, RET
integration and gravitational dynamics remain separate. Book/clock deferrals
are unchanged. Passing this finite gate does not select a physical law.

Publication scope is this eleven-file verification bundle, the two-file
preceding design and the live research roadmap: fourteen files. The previous
published head is `3b992408485fbd49902c1ee42623f44013e23c60`. The 231 unrelated
dirty core/RET/application entries are excluded. Historical local-state
notes in the now-frozen design retain their earlier checkpoint meaning.
No RET API/dependency, temporary model sheet or older frozen source is edited.
