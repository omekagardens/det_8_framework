# QR-MAP — licensed G with committed P-snapshots

12 September 2026. Owner-authorized mapping revision; definition, no executor.
The [finite-birth finding](../validation/t8-q-licensed-growth-2026-09-12/BIRTH_F.md)
and [stop decision](QUANTUM_RECORD_GRAVITY_BRIDGE_PLAN.md) govern this sitting.

## Frozen definition

```text
L, 𝔇 → commit → append maximal event + record
              → snapshot (V, ≺, R, #)
              → question-relative summary
              → optional T7 check on the snapshot
```

**Licensed G:** V contains only committed event entries. Alternatives in Ω
are possibilities, not future vertices. Only at commit is a fresh maximal
event appended, with past an allowed ideal S of the committed poset. Existing
records and relations remain unchanged. L is held fixed; its outputs may
depend on the permitted local record R restricted to S. The selection of S
and outcome must itself be a kernel sample, not an external choice.

**P-as-record:** (V, ≺) in Track A is read here as the committed trace, not
a block of not-yet-facts. A snapshot is finite and past-closed; # counts its
entries, not a scheduler's physical time. Snapshot access does not imply
global physical access or a shared now. Confluence, QR-01 comparisons and T7
diagnostics concern snapshots, not an observer foliation. No MODEL_CARD
primitive is changed.

Block-P (adding uncommitted future slots) and naive-G (ambient birth index as
law or global now) are rejected. “Growing” names trace accumulation, not an
assertion that past events still exist. This leaves the Status-M ontology
unchanged.

Retain 𝔇's relational quantum information and the required full
record-conditioned quantum update, including settings, outcomes and zero
branches. Scalar probabilities or discarded-outcome channels cannot replace
this payload. Ordinary quantum instruments are comparison language, not
inserted primitives. The missing native update is explicit in the finding.

A summary is relative to a declared question family Q: every retained
question's answer must factor through it. Adequacy for one Q implies nothing
about another; qubit-free identifiability entries are atlas, not QR-MAP.

## Frozen evidence and boundaries

- Records belong in the object: [QR-01 Z/X](../validation/qr-01-quantum-records-2026-09-05/RESULTS.md)
  and [QR-05A](../validation/qr-05a-quantum-births-2026-09-05/RESULTS.md).
- Payload maps are not order: QR-05A's equal-map fork/join remain distinct.
- Order diagnostics are not local geometry: [QR-05D's active warp](../validation/qr-05d-geometric-correspondence-2026-09-05/RESULTS.md)
  (u,v) → (u²,v²) holds the flat metric convention fixed.
- Correspondence distance is not bijection error: [12 September](../validation/qr-05-correspondence-noncollapse-verification-2026-09-12/RESULTS.md)
  gives 1/10 versus 1, without a half factor.
- Supplied Minkowski geometry, Johnston kernels and Myrheim–Meyer references
  are fixtures/checks, not inputs. [T7 boundary](gravity.md#3-what-is-borrowed).
- Schedule/birth-label invariance is not Lorentz covariance; classical
  mixtures of order births are not coherent sums over orders.
- Agency is not a variable. Option B and Status M remain unchanged.

This freezes the licensed architecture, **not an already specified birth F
or a geometry derivation**. No empirical or gravity claim follows.
