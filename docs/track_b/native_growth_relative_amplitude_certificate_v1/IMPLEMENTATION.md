# RI-95 — fixed positive-amplitude obstruction certificate

25 September 2026 UTC. **Source preparation only; no native execution.**
Implement only the coordinator-accepted [RI-94 proof/design](../native_growth_relative_amplitude_v1/AMPLITUDE.md).
Its 25,150 bytes have SHA-256
65645454993a81b70a0d9cd278eb137cd6b218055b1cb026f9950b7cb8895844.
The accepted analytic conclusions are not new numerical results of this
checker. In particular, the full positive-amplitude interval remains
unresolved until a genuinely admitted computation and independent audit.

The only reserved repository edits are this protocol and check.py.
No new CERTIFICATE.json is created during preparation. Accepted sources,
historical evidence, RI-93 measurement work, coordinator records and RET
remain unchanged. All git/index, publication, admission and adjudication
are coordinator-owned.

## 1. Fixed question and mathematical scope

RI-91 rejected record-independent relative h8 for the accepted epsilon=1/4
seed in the positive common-level-scale RI-38 continuation class against
the fixed half-scale baseline. RI-94 extends the parameter symbolically:

\[
 z_j=4(H_j-1),\quad h^\epsilon_7(T_j)=1+\epsilon z_j,\quad
 0<\epsilon\leq E=\tfrac14.
\]

All eleven supported classes are unchanged. Complete harmonicity,
positivity and finite-width gain persist over that interval. The
untouched RI-79 contrast still makes t=s necessary for record-independent
relative weights. The remaining test fixes Pstar=C3 ordinal-sum A4 and
records 0,1, with rho=a6 and s=a7 formal and unknown.

Write D_r=V_r-U_r and F=D1-D0-s(D1*U0-D0*U1). The source must prove,
and retain coefficientwise evidence for, the prescribed division

\[
 D_r=\epsilon\rho^2d_r,\quad
 A=d_1-d_0,\quad C=d_1U_0-d_0U_1,\quad
 G_i=2(1+U_i)A-C,\quad F=\epsilon\rho^2(A-sC).
\]

These A,C,G are divided quantities; the RI-91 saved witness uses the
undivided counterparts before its rho-squared quotient. That distinction
is explicit in the serialization and boundary regression.

The baseline outer bounds are
R=1/[2(1+max(E0,E1))] and b_i(rho)=1/[2(1+U_i(rho))].
They are independent of epsilon and are not actual global scale choices.
The domain is 0<rho<=R, 0<epsilon<=1/4,
0<s<=min(b0,b1). Raw F vanishes at epsilon=0; that boundary is excluded.
rho=0 and s=0 are also excluded; rho=R, epsilon=1/4 and the binding
upper s endpoint are included. No positive raw margin through an excluded
axis, actual numerical scale, global maximum, q6/q7 table or catalogue is
computed or claimed.

Passing the fixed sufficient sign certificate rejects the entire interval
only within this record-blind relative-weight continuation class.
Failure of that certificate does not prove a root, a surviving amplitude,
a valid h8, or invalidity of either separate RI-38 growth law.

## 2. Exactly two authenticated retained data inputs

Authenticate complete raw bytes before JSON parsing or arithmetic:

| Relative sibling input | Bytes | SHA-256 |
| --- | ---: | --- |
| native_growth_four_vertex_cap_check_v1/CERTIFICATE.json | 1828149 | ad029d61adc2c8edbe4ae2c3c0969310762a70b411497d4404aa3b3c504f0f5b |
| native_growth_relative_obstruction_v1/CERTIFICATE.json | 364312 | 9e48c437e90cbeef6284ddbcdf5ec915aa520217172f580ef900e5c2bccd37ed |

RI-88 is the sole source of new polynomial construction. Use exactly its
six complete held rows, thirty-two probability slots, and H1,H2,H3:

| Held order | Records | Exact individual ideals | Slots |
| --- | --- | --- | ---: |
| C3=(0,1,3) | 0,1 | 0,1,3,7 | 8 |
| C4=(0,1,3,7) | 0,1 | 0,1,3,7,15 | 10 |
| H5=(0,1,3,7,7) | 0,1 | 0,1,3,7,15,23,31 | 14 |

Retain all eleven-support ordering and inherited prefix/source provenance.
No C5 probability or other marking is extracted for new arithmetic.
Other authenticated rows/coordinates are provenance only. Preserve
complete positive normalization, H5 twin-top equality, source-specific
record transport and H1=5/4. Recover z as 4(H-1), not as H-1 or a newly
renormalized null vector.

RI-91 is solely the fixed boundary-regression reference: three accepted
A/rho^2 Bernstein coordinates and seven for each Gi/rho^2, with the
same authenticated R, parent, records, source and basis semantics. Do
not use its stored native polynomial coefficients to construct U,V,D,A,C
or the bivariate certificate. A complete byte pin does not excuse
wrong index, source or factor-four extraction.

All JSON guards reject duplicate keys, decimals/nonfinite values,
noncanonical rational strings, boolean-as-integer indices, changed
schemas or input identities, missing/reordered selected rows and slots,
and partial saved evidence. Guards must survive optimized Python.

## 3. Two independent bounded reconstruction routes

The closed route implements RI-94 equations 8--13, including the binomial
polynomials L_m(z,epsilon)=sum_{k=1}^m binom(m,k)z^k epsilon^(k-1).
U is degree at most (4,0); D is at most (4,4) and divisible by
epsilon*rho^2. The divided A is at most (2,3), and C,G0,G1 at most
(6,3). These are analytic caps, not degrees fitted after evaluating signs.

The individual-deletion route keeps the frozen lower parent
P6=C3 ordinal-sum A3 and upper P7=C3 ordinal-sum A4:

| Route per two records | Individual proper slots | Raw deletion factors |
| --- | ---: | ---: |
| Lower P6 | 20 | 80 |
| Upper P7 | 36 | 220 |

Retain every labeled ideal, omitted-maximal subset, alternating exponent,
kept vertices, induced order/ideal and transported record, even when
different slots have the same unmarked image. No automorphism division
or orbit merging is allowed.

Independently reconstruct lower proper coefficients and their complete
sum E_r before the upper route uses its full six-parent factor
1-rho*E_r. Do not reuse closed E_r as the supposed independent lower
answer. This full complement is unchanged by h7 because its child has
a unique maximum. It is not an invertible monomial denominator.

Only singleton upper deletions reach the modified six-parent layer.
Multiply their proper factors by 1+epsilon*z_j for the exact induced
supported child; smaller held factors stay unchanged. The expanded
changed-slot product must agree with the direct terminal-multiplier
power, then complete slot sums must agree coefficientwise with the
closed U,V,D. Preserve the four/six/four cap multiplicities and all
four stem ideals.

The generic route may use Laurent intermediate bookkeeping, but only
justified monomial factors can be inverted. No negative power of rho
or epsilon may remain, and nonmonomial/full-complement inversions
must be refused. Different storage alone is not independent mathematics;
the deletion construction and closed formulas must have separate paths.

Before fixed division, every coefficient with rho exponent below two
**or** epsilon exponent below one must vanish exactly. Check all such
positions, not just the constant entry. Refuse higher degrees rather
than trimming, fitting, cancelling another factor or enlarging the cap.

Retain direct cross multiplication using both D and V, raw and divided
forms, and the exact polynomial identities for both full-complement
endpoints. No full-birth denominator is dropped.

## 4. Fixed tensor certificate and outcomes

Use x=rho/R, y=epsilon/E. Pad A to a 3-by-4 grid and each Gi to a
7-by-4 grid. Store rho index first and epsilon index second throughout.

For Q=sum c_ab rho^a epsilon^b, at degrees (m,3), form

\[
 \beta_{ij}=\sum_{a\leq i,b\leq j}
 c_{ab}R^aE^b
 \frac{\binom{i}{a}}{\binom{m}{a}}
 \frac{\binom{j}{b}}{\binom{3}{b}}.
\]

Independently expand the basis products to recover the complete scaled
ordinary grid and divide by the correct R^a E^b to recover the ordinary
grid. Neither repeating the forward transform nor comparing scaled
coefficients to unscaled coefficients verifies this reverse identity.
Every padded zero and both axis powers are part of the evidence.

There are exactly 68 sign coordinates: 12 for A, 28 each for G0,G1.
Require all nonnegative, with each Gi's (6,3) corner strictly positive.
A is weak, not strict. The corner term beta_(6,3)*x^6*y^3 is positive
throughout the half-open rectangle, including its top and right edges.
Zeros restricted to either excluded axis are permitted.

The full seventeen-entry last-column regression is mandatory:
beta_(i,3)=4 times the corresponding accepted RI-91 coordinate.
All seventeen are strictly positive by the accepted reference. Refuse
wrong factor, basis, R, index, identity or positivity before classifying
a native result. A zero/negative family, blind family or zero included
corner contradicts that anchor and is a reconstruction refusal.

The only native mathematical outcomes are:

- certified-positive-amplitude-obstruction: all fixed sign tests pass.
- unresolved-fixed-bivariate-bernstein: at least one required coefficient
  is negative, after all reconstruction and boundary checks pass.

Retain every failing name/index/value deterministically in order
A,G0,G1, then increasing rho index, then epsilon index. An unresolved
outcome makes no root or surviving-amplitude claim. There is no native
negative-orientation, blind-pair, root-diagnostic or automatic sequel branch.
No interval splitting, degree elevation, added record/parent, approximate
tolerance or adaptive search may follow a failure.

Input, provenance, reconstruction, code and resource failures are refusals,
not either mathematical outcome. A printed success without genuine zero
exit and complete supervision evidence cannot count as accepted execution.

## 5. Frozen algebra fixtures and intended-reason controls

The source declares an exact ordered fixture and refusal inventory,
including uniqueness/count checks. Preparation does not execute either.
All tests use synthetic algebra or mutations of the fixed authenticated
domains; they do not query another native parent or choose global scales.

Retain all nine RI-94 fixtures:

1. Positive A=1,C=0.
2. Generic negative A=-1,C=0, separately from the native positive anchor.
3. Weak A=0,C=-1; excluded s=0 does not defeat positive s.
4. Generic blind A=C=0, never native continuation acceptance.
5. A=1,C=8, with a zero at the included binding upper s endpoint.
6. A=1,C=6: conservative two-endpoint failure despite positive F over
   the actual synthetic s interval.
7. A=(y-1/2)^2+1/16,C=0: positive polynomial but mixed tensor coordinates.
8. A=(2y-1)^2,C=0: positive amplitude endpoint with an interior zero.
9. U0=1,U1=1+x^4,d0=d1=x^2*y^3, yielding A=0 and
   G0=G1=x^6*y^3: only the corner coefficient needs to be positive.

For fixtures 1--8 use U0=1,U1=3,
d0=(A-C)/2,d1=(3A-C)/2, and verify those reduction identities.
Synthetic R=1 and E=1/4 are algebraic test scales, not native scales;
x=rho and y=epsilon/E=4*epsilon in every listed fixture.

Explicit additional tests retain G=1-x and G=1-y for included-edge
zeros; the ninth fixture covers both excluded axes. The unequal-axis
polynomial Q=rho^2*epsilon+2*rho*epsilon^3 tests swapped coordinates
and both scaling powers, with complete direct reverse reconstruction.
Generic fixtures cannot
bypass the seventeen-entry anchor in native classification.

Intended-reason mutations cover input/source/proof identities and shape;
full eleven-support order; six rows and full slots; normalized positivity
and twin-top invariance; z extraction; fixed parent/record domain;
forbidden global or q6/q7 requests; lower and upper factor multiplicity,
signs and transports; changed full complement; monomial-only inversion;
raw factor and degree violations; wrong R/epsilon ceiling or endpoint
semantics; padded grid dimensions, booleans, axis swap and incomplete
forward/reverse arrays; factor-four slice and strict corner; forged sign,
root or selector claims; omitted/extra saved fields and partial evidence.

Each negative control must fail for its declared reason rather than
an earlier incidental error. Internal exceptions, absent inputs and
resource exhaustion cannot be counted as a successful refusal control.
Counts and complete ordered names belong to the stable source inventory
below, after implementation review.

## 6. Prospective execution layout and commands

Reserved external preparation root:

/Volumes/AI_DATA/development/det-review-evidence/ri95-qr-source-00XpNr

The prospective target is

/Volumes/AI_DATA/development/det-review-evidence/ri95-qr-source-00XpNr/closure/native_growth_relative_amplitude_certificate_v1/check.py

Its only data siblings are closure/native_growth_four_vertex_cap_check_v1/
CERTIFICATE.json and closure/native_growth_relative_obstruction_v1/
CERTIFICATE.json. Their original repository files and exact closure
copies, plus original/copied checker, must all be frozen and rechecked.
The accepted proof and this protocol are source-review premises, not
additional native data sources. No inherited checker/prefix is imported
into the execution closure.

The exact child cwd is

/Volumes/AI_DATA/development/det-review-evidence/ri95-qr-source-00XpNr/closure

Prospective child argument arrays are fixed:

- Witness: /opt/homebrew/bin/python3, -I, -S, -B,
  /Volumes/AI_DATA/development/det-review-evidence/ri95-qr-source-00XpNr/closure/native_growth_relative_amplitude_certificate_v1/check.py,
  --witness.
- Saved normal: /opt/homebrew/bin/python3, -I, -S, -B,
  /Volumes/AI_DATA/development/det-review-evidence/ri95-qr-source-00XpNr/closure/native_growth_relative_amplitude_certificate_v1/check.py.
- Saved optimized: /opt/homebrew/bin/python3, -I, -S, -B, -O,
  /Volumes/AI_DATA/development/det-review-evidence/ri95-qr-source-00XpNr/closure/native_growth_relative_amplitude_certificate_v1/check.py.

Each array is a specification, not a shell command run during preparation.
The child accepts only witness or fixed saved mode; arbitrary targets,
data paths, scale overrides or extra modes are forbidden.

The exact child and monitor environment is the same five-entry mapping:
PATH=/usr/bin:/bin, LANG=C, LC_ALL=C, TZ=UTC,
__CF_USER_TEXT_ENCODING=0x1F5:0x0:0x0.
Do not inherit additional variables or omit the explicit Darwin entry.
Freeze the interpreter and /bin/ps by complete identity, including
resolved paths and literal symlink chains, immediately before admission.

The historical envelope remains 120 active-child seconds and 536870912
bytes sampled owned-process-group RSS, with 50-ms sample waits and
250-ms maximum monitor calls additionally bounded by remaining wall time.
Sampling is not an allocator hard cap. Preserve internal exact resource/
alarm checks through completed serialization and flushed output;
a late nonzero exit invalidates a prior printed result. No runtime or
memory success estimate is claimed from source preparation.

Exclusive attempt paths are external-root/witness-01, normal-01 and
optimized-01, each with retained stdout.log, stderr.log, samples.jsonl,
prepared.json, admission.json and receipt.json. The supervisor itself
must be separately source-reviewed and qualified or explicitly accepted
for carry-forward; this protocol supplies no supervisor or authorization.

Before witness admission the new original and copied CERTIFICATE.json
must both be absent. The checker writes only stdout/stderr, never a
certificate or source file. Witness stdout is the complete deterministic
certificate plus its exact newline. Saved modes reconstruct the full
witness and compare all fields/types/arrays, not just disposition, then
emit a compact verification summary. Scientific progress stderr is
judged by content; monitor stderr retains its separate empty requirement.

Only after accepted witness custody may the coordinator issue the
saved-certificate addendum, retaining unchanged genuine stdout as the
candidate at the fixed repository/copy paths. Normal and optimized runs
each need their own fresh exact freeze, authorization and exclusive
attempt. They are producer replays, not independent arithmetic.

Every attempt requires source/data/runtime/monitor identities, exact
ordered input roles and paths, cwd/argv/environment/limits, and the
authorization tied to that concrete freeze. Counts/sets cannot replace
ordered inventory equality. A null prospective authorization is not
admissible. Preserve actual outer tool completion and exit, raw samples,
live-child sampling, final process-group absence and complete before/
after/output identities. Receipt success alone is insufficient.
Preserve any failed attempt; there is no automatic retry or relaxation.

## 7. Independent arithmetic is a separate required dependency

A later independently written, separately reviewed data-only consumer
must reconstruct the entire accepted candidate, not import this checker
or trust its sign flags. It must independently derive the fixed held
slot/transport products, all retained polynomials, raw valuation, all grids and
reverse expansions, endpoint identities, seventeen-entry regression
and disposition, then reconcile complete canonical witness bytes.

This consumer and its exact caller/descriptor/source/input/resource/
authorization packet are not created or executed by this source-only
assignment. Its absence is explicit, not substituted by normal/-O
producer replay. Its reviewer must distinguish declared producer
refusal names from controls actually executed by the producer.

Final mathematical acceptance remains conditional on the inherited
RI-88 probabilities and RI-89/94 theorem premises, genuine successful
producer custody and independently accepted complete reconstruction.
An internally reviewed finite exact certificate is not an external
peer review, proof-assistant derivation, empirical result or gravity
prediction.

## 8. Source inventory and handoff

The checker is frozen at 1213 lines / 79250 bytes, SHA-256
572b156ce1f8833a23288e2b079cf48c4a1742e8e847b3bacb5652879d772639.
Its scientific schema is ri95-relative-amplitude-certificate-v1.
The source declares exactly 12 fixtures and 134 unique ordered
intended-reason refusals. Static AST inspection of source text, without
importing or executing any target or producing an executable target code
object, confirms syntax and independently expands the metadata-only
control-name loops to the exact declared list. It does not run a guard,
fixture, polynomial reconstruction or saved replay.

The seventeen top-level certificate fields are:

- schema
- design_sha256
- design_bytes
- accepted_inputs
- inputs
- selected_parent
- records
- scale_domain
- cross_multiplication
- full_complement_s_endpoints
- fixed_division_evidence
- tensor_evidence
- decision
- coverage
- limitations
- checker_sha256
- refusal_controls

The seventeen-entry boundary regression is nested under decision.
Sparse polynomial terms are serialized in increasing (rho,epsilon)
order with exact reduced-rational strings; zero terms are omitted there.
Padded ordinary/scaled/tensor/reverse grids instead retain every zero
and their exact dimensions. Saved verification compares full canonical
bytes with exactly one newline, in addition to full recursive exact
types and values. Normal/-O summaries are not independent evidence.

The twelve fixed fixture names, in order, are:

1. positive
2. negative-generic-orientation
3. weak-A-zero
4. blind
5. included-binding-s-endpoint
6. conservative-both-endpoints
7. positive-mixed-amplitude-basis
8. endpoint-positive-interior-zero
9. strict-corner-only-support
10. included-rho-edge-zero
11. included-epsilon-edge-zero
12. unequal-axis-scaling

The complete declared refusal sequence, independently matched to source
call order including its three fixed name loops, is:

1. duplicate-json-key
2. decimal-json
3. nonfinite-json
4. unreduced-rational
5. boolean-rational
6. wrong-ri88-size
7. wrong-ri88-hash
8. wrong-ri91-size
9. wrong-ri91-hash
10. wrong-ri88-source
11. wrong-actual-prefix
12. bool-provenance-count
13. extra-ri88-field
14. missing-ri88-field
15. changed-full-eleven-support-order
16. missing-full-eleven-H-provenance
17. reordered-selected-rows
18. extra-selected-record
19. bool-selected-record
20. bool-held-order
21. missing-held-full-slot
22. duplicate-held-slot
23. nonpositive-held-probability
24. changed-held-locality
25. changed-twin-top-invariance
26. changed-H1
27. changed-H-bound
28. wrong-direction-factor-four
29. bool-direction-value
30. numeric-six-parent-query
31. numeric-seven-parent-query
32. C5-query-refusal
33. nested-unhashable-order
34. extra-record-query
35. no-actual-rho
36. no-actual-epsilon
37. no-actual-s
38. no-actual-a6
39. no-actual-a7
40. no-actual-M6
41. no-actual-M7
42. no-actual-q6
43. no-actual-q7
44. full-top-ideal-not-proper
45. delete-nonmaximal-vertex
46. bool-deletion-mask
47. bool-symbolic-precursor
48. wrong-independent-E
49. missing-lower-slot
50. wrong-lower-factor-exponent
51. missing-top-slot
52. duplicate-top-slot
53. collapsed-labeled-slot-weight
54. wrong-transported-record
55. wrong-proper-locality-record
56. missing-singleton-amplitude
57. wrong-amplitude-individual-power
58. modified-symbolic-full-complement
59. zero-deletion-exponent
60. invert-amplitude-binomial
61. invert-full-complement-binomial
62. surviving-negative-Laurent-power
63. bool-polynomial-power
64. bool-polynomial-coefficient
65. polynomial-global-bidegree-expansion
66. polynomial-duplicate-power
67. polynomial-explicit-zero
68. raw-epsilon-zero-coefficient
69. raw-rho-zero-coefficient
70. raw-rho-one-coefficient
71. raw-D-rho-degree-expansion
72. divided-A-degree-expansion
73. divided-G-amplitude-degree-expansion
74. selected-bound-as-half
75. changed-amplitude-ceiling
76. zero-tensor-radius
77. adaptive-tensor-degree
78. bool-tensor-degree
79. transposed-tensor-axis-order
80. changed-fixed-tensor-degree
81. missing-tensor-row
82. missing-tensor-column
83. extra-tensor-column
84. bool-tensor-coordinate
85. altered-tensor-coordinate
86. wrong-amplitude-scaled-power
87. wrong-reverse-sign
88. scaled-is-not-original-grid
89. omitted-zero-padding
90. wrong-upper-edge-identity
91. wrong-included-corner
92. missing-second-G-sign-grid
93. false-sign-positive
94. false-sign-negative-generic-orientation
95. false-sign-weak-A-zero
96. false-sign-blind
97. false-sign-included-binding-s-endpoint
98. false-sign-conservative-both-endpoints
99. false-sign-positive-mixed-amplitude-basis
100. false-sign-endpoint-positive-interior-zero
101. false-sign-strict-corner-only-support
102. false-sign-included-rho-edge-zero
103. false-sign-included-epsilon-edge-zero
104. missing-ordered-failure
105. reordered-failure-indices
106. wrong-failure-value
107. bool-failure-index
108. wrong-ri91-source
109. wrong-ri91-input-provenance
110. wrong-ri91-parent
111. bool-ri91-record
112. wrong-ri91-radius
113. wrong-ri91-degree
114. missing-ri91-anchor-entry
115. zero-ri91-anchor
116. wrong-endpoint-factor-four
117. anchor-positivity-without-equality
118. wrong-new-schema
119. extra-new-certificate-field
120. missing-new-certificate-field
121. noncanonical-saved-serialization
122. bool-new-record
123. missing-retained-input-row
124. wrong-raw-divisor-rho
125. wrong-raw-divisor-epsilon
126. forged-raw-zero-coordinate
127. wrong-full-complement-s-endpoint
128. full-complement-endpoint-is-not-actual-s
129. claim-native-negative-branch
130. claim-native-blind-branch
131. claim-native-root-branch
132. claim-numeric-actual-scale
133. claim-selector-acceptance
134. claim-independent-regression

During source review, two mutation tests initially reached recursive
list-shape checks before their claimed amplitude/complement reasons.
Explicit semantic guards now precede that shape comparison. The final
source also contains normalization-preserving locality and twin-top
mutations and explicit evidence/mutations for both full-complement
s endpoints. These are corrected source designs, not failed scientific
executions or controls claimed to have run.

Both independent reviewers read the complete 1,213-line final source
and reconciled the 134 controls and 12 fixtures at the identity above;
both source-only reviews passed without an unresolved finding. Main
review also checked the complete source and changes, and independently
matched the ordered inventory by nonexecuting AST metadata inspection.
These are source reviews, not executed-test results or admission.
The author has stopped edits at the identity above. Any subsequent
source change invalidates that frozen identity and must be reconciled
before publication or admission. The external root in section 6 is
only reserved; closure staging, supervisor source/qualification, concrete
freeze/authorization, and actual execution are not supplied by this packet.

No actual new native coefficients, scales, global maxima or probability
tables are computed in this stage. No certificate is created and no
target source is imported, compiled to an executable code object or
executed. Root owns source acceptance, publication, concrete admission
and result adjudication. A stable source handoff completes this bounded
obligation, not the overall programme; any next bounded assignment
remains separate.
