# RI-63 — exact first-free-layer canonical completion

24 September 2026 UTC. **Independently accepted finite certificate; coordinator
adjudication complete.** This packet completes parent size five of the unchanged
[RI-59 recursion](../native_growth_expected_defect_v1/EXPECTED_DEFECT.md).
It does not define a parent-six probability law. Six-event orders appear only
as terminal orders for intrinsic defect diagnostics.

## 1. Result and scope

The exact first-free-layer minimum is

\[
 \boxed{m_5=\frac{6650803}{108900000}\simeq0.06107257116620753.}
\]

[CERTIFICATE.json](CERTIFICATE.json) contains a rational primal vector, a
rational primary dual, **69 sequential optimal-face dual certificates**
selecting RI-59's canonical lexicographic minimizer, and a distinct feasible
primary optimum. [check.py](check.py) reconstructs the finite problem from
the byte-pinned RI-41 prefix and checks every certificate equality and
inequality exactly. No floating tolerance or optimizer status enters acceptance.

The primary optimum is **not unique**. The canonical vector has 69 positive
coordinates; the alternative has 66, with 56 differing coordinates. Both
are exactly feasible with equal objective. For example, canonical coordinate
109 is zero while the alternative's is `8800/1313`. The 76 positive primary
dual rows and 112 negative-cost zero-slack columns alone would not prove
nonuniqueness; the two supplied exact vectors do.

For the canonical **boundary** law, all 69 active components are everywhere
defect-neutral. Using the notation of
[RI-61](../native_growth_expected_defect_suppression_v1/SUPPRESSION.md),

\[
 A_5^{\mathcal C}=A_5^{\mathrm{out}}=F_5^{\mathrm{out}}=0,
 \qquad F_5^{\mathcal C}=m_5. \tag{1}
\]

Every one of the 1,198 noncritical marked-row classes has zero boundary drift.
These are exhaustive **finite-layer** results, not all-size neutrality,
decay, or emergence theorems. The actual strict mixture has positive drift
outside the critical class and is not the boundary optimizer.

## 2. Fixed inputs and complete reconstruction

The seed, RI-41 parent-four completion, objective, normalization and canonical
ordering are unchanged. The checker reads the actual
[RI-41 certificate](../native_growth_height_normalization_v1/CERTIFICATE.json),
requires SHA-256
`3d966fb20911d630a427ceb777a41539e930d1ba75c3098dcad35e08e4b1d969`,
and reconciles every embedded coefficient and root. The canonical parameter
JSON hash is `465c5de48c69527fc59e8df1f494fd8cea33f8aec243f59b9b2820bd5b1c07a7`.
Prefix formulas are re-expressed without project imports.

Every naturally labeled order is generated by ideal births and independently
checked against all forward-relation masks, retaining exactly the transitive
ones. Counts through size five are `1,1,2,7,40,357`. All binary records remain.
A marked natural history has weight equal to the product of actual prefix
birth probabilities and one factor `1/2` per newborn bit. Canonical marked
rows aggregate these weights with their natural-history multiplicities.
They are not uniformly weighted isomorphism classes or a single realized path.
Total history mass is exactly one at every size through five.

The local node retains the entire unmarked parent and only precursor marks.
Its key is the minimum transported `(E(P),mask(S),mask(r|S))` over all vertex
permutations. Proper components are ordered by their minimum key. Every
proper node is seeded before diamonds are joined, including possible isolates.
Every labeled ideal occurrence contributes to row sums, including repeated
canonical node keys. RI-38's fixed maximal-deletion potential is

\[
 u_{P,r}(S)=\prod_{\varnothing\ne H\subseteq\operatorname{Max}(P)\setminus S}
 q_{P\setminus H,\,r|_{P\setminus H}}(S)^{(-1)^{|H|+1}}. \tag{2}
\]

The graph includes every base-four order, all 16 old records, every ordered
precursor pair and both newborn bits on both sides. Equal precursors, loops
and all-zero records remain included.

| Reconstructed domain | Count |
| --- | ---: |
| Natural parent-five orders | 357 |
| Raw marked rows / canonical marked-row classes | 11,424 / 1,490 |
| Proper labeled slots / all labeled slots | 142,944 / 154,368 |
| Proper local nodes / components / unincident nodes | 2,961 / 798 / 0 |
| Complete new-layer diamonds | 216,128 |
| All-zero / equal-precursor / loop diamonds | 3,377 / 22,848 / 28,176 |
| Negative / zero / positive objective coefficients | 125 / 262 / 411 |
| Critical parent classes | 292 |
| Everywhere-raising / critical-child components | 347 / 347 |
| Terminal six-event orders | 4,824 |

Equality of the two component counts is finite evidence, not an all-size
classification. Prefix admission checks all 707 marked rows and 6,065 slots
through parent four, 585 canonical probability keys, 436 lower diamonds and
7,616 parent-four diamonds.

The defect is minimum **arbitrary vertex** deletion distance to a Ferrers
cell order, not ideal-only or maximal-only deletion distance. All integer
partitions through six and all their natural order realizations are generated.
Deletion recursion is crosschecked against direct minimization over every
retained subset of every terminal order: 4,824 orders and 308,736 subsets.
The terminal inventory separately equals the transitive orders among all
32,768 forward relation masks. A Ferrers `V` deleting to a non-Ferrers
antichain is an explicit nonheredity control.

## 3. Exact primary and canonical proof

Let `j` index marked-row classes and `c` canonical proper components; `pi_j`
is actual history mass, `d_j(S)` the defect increment, and `f_j=d_j(P_j)`.
Reconstruct from every labeled ideal slot

\[
 A_{jc}=\sum_{S:c(j,S)=c}u_j(S),\quad B_5=\sum_j\pi_jf_j,\quad
 \beta_c=\sum_j\pi_j\sum_{S:c(j,S)=c}(d_j(S)-f_j)u_j(S).
\]

The unchanged problem is

\[
 \min_{\alpha\geq0,\ A\alpha\leq\mathbf1}
 \Phi_5(\alpha)=B_5+\beta^T\alpha. \tag{3}
\]

RI-61's downward-closed elimination gives zero for positive-cost coordinates
in every optimum, and zero for zero-cost coordinates in the **canonical**
optimum. It does not imply that zero-cost coordinates vanish in every primary
optimum. Removing all `beta>=0` coordinates preserves the canonical solution;
the final verifier still checks the original full problem.

The primary certificate has `y>=0` with

\[
 s=\beta+A^Ty\geq0,\qquad\beta^T\alpha=-\mathbf1^Ty. \tag{4}
\]

Primal feasibility and weak duality certify optimality. Complementarity is
also checked: coordinates with `s_c>0` vanish and rows with `y_j>0` are tight
on the primary optimal face.

For positive canonical coordinate `k`, substitute all earlier coordinates at
the proposed canonical values. The remaining columns satisfy `beta_c<0`,
`s_c=0`, `c>=k`. Set `b_j=1-sum_(c<k) A_jc alpha_c`; let `F` be the
remaining-column matrix of primary forced-tight rows and `h` their RHS.
The stage supplies `v>=0` and signed `z` such that

\[
 e_k+A_{\mathrm{rem}}^Tv-F^Tz\geq0,\qquad
 -v^Tb+z^Th=\alpha_k. \tag{5}
\]

For every feasible remaining vector `x`, these imply
`x_k >= -v^T b + z^T h`. The proposed vector attains this bound. A zero target
needs only nonnegativity. Induction through all 798 coordinates proves the
specified lexicographic minimum. Exactly 69 positive-coordinate stages are
required; every gradient and bound is checked using `Fraction`, not rank or
floating tolerances. The distinct alternative is separately checked for
feasibility and equal objective.

## 4. Boundary and actual strict-law quantities

The unchanged strict completion is

\[
 \eta_5=1/36,\quad M_5=\max_j\sum_cA_{jc},\quad c_5=1/[2(1+M_5)],
 \qquad \widetilde\alpha=\frac{35}{36}\alpha^{\min}+\frac{c_5}{36}\mathbf1,
\]
\[
 q_j(S)=u_j(S)\widetilde\alpha_{c(j,S)},\qquad
 q_j(P_j)=1-\sum_{S\subsetneq P_j}q_j(S). \tag{6}
\]

Exact normalization:

\[
 M_5=\frac{3654565502781806879423940938688691429}{465217116774076383750000000},
 \quad c_5=\frac{232608558387038191875000000}{3654565503247023996198017322438691429}.
\]

Every proper slot is positive and every full complement exceeds
`eta5/2=1/72`. The verifier replays all 11,424 strict marked rows and 216,128
scalar-map diamonds, the marked-local quotient, and the raw-history expected
drift. Rational birth coefficients are not unitary amplitudes or a newly
reconstructed quantum instrument.

| Quantity | Exact value or definition | Approximation |
| --- | --- | ---: |
| All-full baseline `B5` | `171076990409/1686643200000` | 0.10143045690339249 |
| RI-61 comparison `epsilon5` | `6902084170944631237/79254150709324800000` | 0.08708798352100129 |
| Boundary minimum `m5` | `6650803/108900000` | 0.06107257116620753 |
| Initial expected defect | `165227968419299/100186606080000` | 1.6492021726673007 |
| Interior drift `Phi5(c5*1)` | Exact rational in certificate claims | 0.10242477548015787 |
| Actual drift `r5` | `(35*m5+Phi5(c5*1))/36` | 0.062221243508261706 |
| Actual terminal expected defect | `E[delta5]+r5` | 1.7114234161755624 |

The exact checker preserves RI-59's weighted lower bound and proves
`2401/450000 <= m5 <= epsilon5 < B5`. The comparison uses every negative-cost
component with common coefficient `1/T5`, where

\[
 T_5=\frac{211451763}{212960000},\quad
 \Lambda_5=\frac{310009240088866697}{21768941568000000000},\quad
 \epsilon_5=B_5-\Lambda_5/T_5.
\]

All large rational actual expectations, extrema, suppressed masses and
critical-child probability are saved under `claims` and recomputed each run.
The contribution split is:

| Contribution | Boundary | Actual strict mixture, approximately |
| --- | ---: | ---: |
| Proper raising, critical parents `A_critical` | 0 | 0.00002761995848243872 |
| Proper raising, noncritical parents `A_out` | 0 | 0.000000000002149641563081088 |
| Full raising, critical parents `F_critical` | `m5` | 0.06107257116609375 |
| Full raising, noncritical parents `F_out` | 0 | 0.0011210523815358754 |

Boundary proper raising contributions could only come from mixed components
by RI-61; at this canonical layer they vanish. **Actual** proper-raising
totals also include interior restoration of eliminated/pure-raising components
and are not boundary mixed-component mass.

Critical parent-five occupation is
`347994494814343/500933030400000`, approximately `0.6946926509047845`.
It is fixed-prefix occupation, not subject to RI-61's later-child occupation
bound. Every `beta>=0` canonical coordinate is zero; strict restoration has
total probability below `1/72` in every row.

### Hook: a negative coefficient need not receive canonical boundary mass

RI-61's all-record hook roles lie in component 98, with

\[
 \beta_{98}=-205/697827328<0,\qquad\alpha^{\min}_{98}=0.
\]

The empty-ideal raising role on `(0,1,3,7,1)` and both neutral restoration
roles are checked for all 32 records each. The strict coefficient is only
`c5/36`. Its empty-ideal probability, the same for all 32 records, is

\[
 \frac{16687367882449365234375}{233892192207809535756673108636076251456}.
\]

Thus the hook proves a feasible descent direction from the all-full vector,
not mandatory allocation by the coupled constrained canonical optimizer.
It is not an infinite-path counterexample or evidence of later residual decay.

## 5. Discovery, resources, and replay evidence

The envelope was declared before computation: 900 seconds per attempt and
2 GiB resident memory. A parent watchdog sampled child RSS every 0.5 seconds
and terminated on either limit. The checker adds a 900-second wall alarm and
peak-RSS checkpoints. These are sampled/checkpoint safeguards, **not a hard
operating-system allocator limit**; instantaneous unobserved overshoot is not
ruled out.

Proposal-only discovery installed SciPy 1.16.3 and NumPy 2.3.5 in the isolated
`/tmp/ri63-proposal.Hzz43L` under bundled Python 3.12.14. No repository dependency,
system environment, or other task's environment changed. It used
`linprog(method="highs-ds")`, a 120-second solver limit, 100,000-iteration
limit and `1e-9` primal/dual proposal tolerances. The
[official HiGHS interface documentation](https://docs.scipy.org/doc/scipy/reference/optimize.linprog-highs.html)
describes these controls; the actual installed version was checked at runtime.

Columns were scaled by their largest row coefficient and the reward by its
largest scaled magnitude. Active supports were reconstructed using rational
Gaussian elimination, then all original primal/dual inequalities, equalities
and stage bounds were checked exactly. There were 77 floating stage proposals
and 69 positive-coordinate certificates. Floating computation is not needed
for final replay.

A separate discovery reconstruction used the read-only, byte-pinned
[RI-45 helper](../native_growth_neutral_completion_v1/check.py), SHA-256
`ee8019b9184ad5282aef8b11c896ad2372d804953584064abda87e718b86b586`,
and independently expressed defect/history/objective calculations. All 1,490
rows, weights, caps and diagnostics and all 798 roots and coefficients were
reconciled exactly with the final standalone reconstruction. The final checker
re-expresses RI-52/RI-41 prefix formulas and imports no project module.

| Guarded run | Wall seconds | Sampled peak RSS, bytes |
| --- | ---: | ---: |
| Proposal inventory and primary floating solve | 3.980 | 163,643,392 |
| Exact primary recovery | 4.167 | 177,717,248 |
| Canonical sequential recovery | 21.725 | 191,725,568 |
| Final capture and independent row reconciliation | 25.533 | 244,727,808 |
| Final main standard-library normal replay | 9.213 | 123,191,296 |
| Final main standard-library `-O` replay | 9.194 | 123,437,056 |

The last two runs used CPython 3.14.0 and the final claims-bearing certificate.
Both exited zero; their actual stdout bytes were compared in memory and were
identical: 4,173 bytes, SHA-256
`f6573f4e056701a15a8b13efc96a04a8510ae3097285f7cbc480d4876da534ab`.

The final checker uses only the standard library, with no optimizer, downloads,
project imports, subprocesses or writes:

```sh
python3 -I -S -B docs/track_b/native_growth_expected_defect_completion_v1/check.py
python3 -I -S -B -O docs/track_b/native_growth_expected_defect_completion_v1/check.py
```

`--problem` emits the reconstructed exact problem but neither optimizes nor
certifies it. Normal invocation verifies the supplied certificate. Explicit
runtime checks remain active under `-O`.

Fifteen malformed-certificate controls require specific refusal reasons:
wrong manifest; infeasible primal; feasible zero primal with positive duality
gap; infeasible primary dual; missing canonical proof; a noncanonical optimum
substituted as the answer; bad stage gradient; bad stage lower bound;
corrupted history weight; corrupted objective; duplicate JSON key;
noncanonical fraction; boolean index; out-of-range index; unknown field.
Four domain controls and Ferrers nonheredity also run during reconstruction.
Claims are compared with reconstructed results, never trusted as input.

Two independent reviewers read the complete final checker and certificate.
The mathematical review found no primary/face-dual, nonuniqueness or finite
scope blocker. The independent execution reviewer additionally ran normal
and `-O` under explicit pre/post source and certificate pins: exits zero,
8.739/8.701 seconds, and the same 4,173-byte stdout/hash above. The main worker
also read the entire final checker and performed the separately guarded
replays above. This review does not replace coordinator adjudication.

| Identity | SHA-256 |
| --- | --- |
| Final checker | `39d64d11c20675c08e430ceeb335f5b879ffdede1b61ed9fa48f402f21e69b2b` |
| Final claims-bearing certificate | `f55625a686b4c859a8b3e720c80fbabbe9cfa0972cb7a9e78a50d1705833c28b` |
| Reconstructed complete problem | `dd65ca6cb186946664436e54e8bdc1ef51693e96929cd66af519d00c2a3dbf0c` |
| Actual strict-probability manifest | `7e27822390387111bf01b3c8e67a12a8b06a9023d2bd3f8bacfa8aeab20c0378` |

Further row, history, node, objective, terminal defect and complete diamond
hashes are reconstructed and matched to `CERTIFICATE.json` each run.

## 6. Remaining obligations and stop boundary

This instantiates a frozen finite definition, proves its exact primary and
canonical solution, and refutes uniqueness of primary optimization. It closes
only the first-free-layer numerical/canonical obligation. RI-61's all-size
noncritical residual `A_n^out+F_n^out`, its Cesaro decay, and the comparison
envelope's decay remain unproved. One zero boundary residual does not resolve
them.

No parent-six probability law, further optimization layer, borrowed quantum
amplitudes, Hilbert reconstruction, physical metric, mass/gravity derivation,
empirical discriminator or DET-native quantum/geometry bridge is supplied.
Option B and metric-as-record Status M are unchanged. RET remains paused;
no measurement, clocks, book, retired kappa-gravity or isolated research work
is modified.

The only packet paths are this directory's `COMPLETION.md`, `check.py` and
`CERTIFICATE.json`. No accepted-source or ledger edit is made by the worker.
Git/index/commit/push, acceptance wording and any successor obligation remain
with the coordinator. Stop at the stable packet handoff.
