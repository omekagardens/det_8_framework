# RI112 independent audit contract — source preparation

26 September 2026. This is a future-execution contract, not an admission,
an executed test or an actual rank result. The producer and consumer sources
must receive complete independent review and root acceptance before use.
No scientific attempt, descriptor, candidate, freeze or authorization is
created by this preparation.

## Purpose and scientific boundary

Independently reconstruct the complete19-section RI112 certificate from
exactly24 accepted held rows /128 probabilities and fixed proof premises.
The family is exactly RI111's28 children at the existing baseline/seed and
epsilon=1/4. The consumer must neither rely on producer numerical flags nor
invoke its arithmetic, prefix, solver, checker or a shared calculation module.

Producer uses bounded Fraction arithmetic, dense coefficient lists and
Horner evaluation. Consumer uses its own normalized integer pairs,
cross-cancellation, sparse polynomial long division with reconstruction
identities, and coefficient-times-power endpoint evaluation.
The inherited RI107 consumer arithmetic source is40917 bytes, SHA-256
0ed91135d1656cdf46efb43e580c888589282166fba889e4c9e0eb5becd55877.
Provenance is exact source reuse, not a claim that new code has been
qualified or run. New extraction, all-record expressions, gcd folding,
synthetic families and interface guards require fresh source review.

## What must be rebuilt

Follow the exact field definitions in IMPLEMENTATION.md, including:

- All24 held entries in shape-major, record-minor order; all individual
  ideal masks, strict positivity, complete normalization, proper-precursor
  locality, empty-record equality, twin-slot equality and accepted transport.
- Eight complete p,E,j,nu,K,U profiles and their component terms.
- Seven exact reference contrasts, preserving padded coefficients and the
  fixed division by rho, without dividing by Delta_p.
- The accepted P01 nonzero pivot rechecked by a complete root certificate
  on the unchanged outer interval using only E0,E1.
- Every one of the six labeled minor coefficient arrays and raw difference
  identities, including zero members and degree drops.
- Deterministic gcd normalization/folding over all nonzero minors, with each
  Euclidean quotient/remainder and multiplication identity; divisibility of
  every minor by the final gcd, including zero members.
- Complete square-free and coprimality evidence, unrescaled negative-remainder
  Sturm steps, endpoint values/signs/variations and distinct real root count.
- All16 root fixtures and all10 six-minor family fixtures from source-defined
  artificial operands, independently reconstructed within future execution.
- Every fixed claim-limit field and every producer refusal declaration;
  declarations are not assertions that the consumer ran producer mutations.

The all-zero branch has null gcd and null root certificate/count; it is
unresolved. A nonzero constant gcd or zero common real roots rejects only
the fixed28-child support. Any surviving root is unresolved, not a located
actual rho, an admissible s, a positive solution or remaining-parent
feasibility. Other fifteen parent systems stay outside this decision.

These results are conditional on accepted scalar-passive/local/marked
premises. No general QM, all-size extension, empirical correspondence,
geometry, gravity, mass, clocks, book or RET result follows.

## Input authentication and no circular trust

The four fixed premise roles are ri88,ri111,ri109_adjudication,
ri111_adjudication, with exact pins in IMPLEMENTATION.md and both sources.
The scientific RI88 body may be parsed only after all declared inputs,
source copies and custody byte identities have been authenticated.
Only the24 selected rows are converted to rational scientific operands.
Remaining probabilities and H/z values are not used; no prefix is replayed.
Check both accepted adjudication schemas/statuses and RI111's historical
UNRESOLVED/no-new-coefficients/no-new-numerical-admission fields. These
describe RI111's analytic result, not permission for the present source
packet or a later numerical attempt.

A future root-created descriptor has six exact keys:
schema,phase,files,accepted_sources,audit_source,custody_dependencies.
Its schema is ri112-independent-audit-input-v1 and phase
fixed_saved_certificate_audit. Every ordinary entry has path,bytes,sha256;
ordered custody entries also have role.

The files group has5 roles: ri88,ri111,ri109_adjudication,
ri111_adjudication,candidate.
The accepted_sources group has6 roles: ri111,ri109_adjudication,
ri111_adjudication,checker,protocol,contract.
Add the actual executing audit_source and exactly5 custody roles:
producer_witness_stdout,producer_witness_custody,producer_normal_custody,
producer_optimized_custody,consumer_source_review.

Thus17 bindings must refer to14 unique payload paths, with exactly three
permitted aliases: the same ri111/ri109_adjudication/ri111_adjudication
objects across files and accepted_sources. Every other path is distinct;
the descriptor has its own distinct path. Candidate and genuine witness
stdout are separate files with equal whole bytes, including newline.

Every path is absolute, normalized and literal, with no symlink component;
every payload is a regular file with positive exact integer length<=8MiB.
Hashes are canonical lower-case SHA-256. Fixed proof/input/source hashes
are checked against source constants, not just accepted from the descriptor.
The actual executing consumer source is captured before descriptor parsing
and must match the descriptor's audit_source identity.

No placeholder source identity is admissible. Producer/protocol/contract
source pins are filled only after source stabilization and before final
source review. The descriptor/candidate/custody inputs themselves remain
absent until root supplies real accepted evidence. No fake acceptance,
prospective hash, manufactured completion or speculative output is allowed.

Pinned producer custody bodies are not self-adjudicated by this arithmetic
consumer. A future separately reviewed root-owned caller must authenticate
their actual semantics, ancestry, receipt and genuine outer-completion
bindings. Fresh precise qualification applicability and explicit root
authorization remain separate requirements. The current historical RI111
analytic acceptance does not grant RI112 numerical permission.

## Completion and failure coverage

Invocation is the fixed consumer source with DESCRIPTOR BYTES SHA256,
under -I -S -B, an exact root-approved environment and bounded supervisor.
The descriptor byte count/hash itself is supplied in the frozen argv.
No descriptor path discovery, target override or dynamic import is added.

Capture source identity before descriptor validation. Snapshot every
distinct payload before scientific reconstruction. Compare every nested
saved field by exact type and exact value to independent reconstruction;
then require complete canonical candidate bytes, not only a selected list
of favorable conclusions or a producer success flag.

Attempt each protected postcheck independently even after an earlier read,
parse, reconstruction or budget failure. Protect all14 unique payloads plus
the descriptor:15 successful postchecks are required for success. Preserve
bounded emergency failure diagnostics, including the own-source attempt
on malformed-descriptor paths. Do not hide a late mismatch, skip later
checks through boolean short-circuiting, or convert a partial reconstruction
into a scientific disposition. A failure is not rejection of the family.

On success emit only the complete report to stdout, with reconstructed
certificate, its byte identity, hashes of all19 sections, exact17 role
bindings and14 payload paths, all15 successful postchecks and actual
consumer counted-operation metadata. Distinguish source-defined producer
controls from the16+10 synthetic cases rebuilt by the consumer.
No report file is written by the target itself; a separately accepted
supervisor may preserve its raw stdout and create an exclusive report copy.

Any child or outer failure invalidates an apparent earlier success.
Actual successful acceptance also needs genuine outer dispatch completion
and zero exit, full process/resource/custody reconciliation and root review.
A receipt flag alone is insufficient. No automatic retry, retuned input,
relaxed resource bound or silent omission is permitted.

## Resource and runtime obligations

Both targets retain bounded exact arithmetic:32768-bit inputs,
1048576-bit working rationals,2097154-bit transient integer limit,
200000 counted operations,20000-character rational text,8MiB input/output.
These are ceilings, not measured usage or a promise the new task fits them.

Retain a120-second active-child envelope and512MiB sampled owned-group RSS,
50ms waits, each process census timeout at most250ms and bounded by the
remaining deadline, at least one live-child sample and final empty owned
group. This is sampled supervision, not a hard allocator cap or an
uninterrupted census. In-target resource checks are supplementary.
No supervisor or qualification campaign is implemented by this assignment.

Root must pin actual interpreter bytes, resolved path and literal symlink
chain, executing sources, protocol/contract, monitor binary, every input
original/copy, candidate/receipt/custody and freeze/authorization identities.
A version string or old hash is not a fresh observation. Any applicable
inherited historical/failed-attempt inventories remain mandatory outside
the arithmetic consumer; this packet does not waive or replace them.

Root decides the actual witness, saved normal/optimized and independent
audit stages, their applicability evidence and single-attempt admission.
No runtime/version probe, Python parse/AST/import/compile, target invocation,
native coefficient computation or synthetic fixture execution occurs in
source preparation.

## Intended consumer refusal coverage

The final source review must trace each of these guard families, without
claiming execution now:

- Invocation, integer-vs-bool sizes, invalid/extra descriptor keys and wrong
  schema/phase/role order/count; source/self mismatch or unbound pins.
- Noncanonical/nonabsolute paths, symlinks, nonregular/oversize/empty files,
  digest changes, descriptor-payload alias or unapproved cross-role alias.
- Missing provenance or fixed input/source/adjudication mismatch before
  scientific parsing; nonidentical candidate and genuine witness bytes.
- Duplicate JSON keys, decimal/nonfinite JSON, noncanonical rationals,
  integer bit/operation/time/resource bounds, zero divisors and wrong degree.
- Incomplete/duplicate/reordered held rows, wrong records/ideals/types,
  nonpositive or unnormalized rows, forbidden record dependence or twin error.
- Any changed profile, coefficient, contrast, minor label/zero member, gcd
  branch/fold/divisibility, square-free evidence, Sturm step or endpoint.
- Forged all-zero/constant/surviving-root disposition; omitted strict scope
  limits; actual-scale, positive-repair or general-extension claims.
- Partial saved-body comparison, missing/retyped fields, noncanonical whole
  bytes, altered source/input/descriptor during work or missing postchecks.

Synthetic refusal actions and their stable names/reasons reside in the
producer; the consumer independently reconstructs that declaration interface.
Its own guard inventory is source review, not a fabricated executed test.
Any later qualification request must be separately specified and admitted.

## Stop condition

Deliver two fully reviewed source files, exact provenance and source deltas,
this contract and implementation specification, complete source/input/runtime
requirements, synthetic declarations and stable independent-review evidence.
Root alone accepts, publishes or admits the next action. This worker task
stops at source handoff; the wider programme continues and RET remains paused.
