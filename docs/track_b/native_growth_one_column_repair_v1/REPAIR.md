# RI-111 — one additional proper child: all-record repair criterion

26 September 2026. Analytic proof/design only. Fixed actual baseline B,
fixed RI-88 eleven-class direction Z, and amplitude epsilon=1/4.
No new probability coefficient, polynomial coefficient, scale, solver,
certificate, fixture or full-layer table is evaluated.

**Result.** Adding exactly Y0=C3 ordinal-sum A5 gives 28 shared child
variables, sixteen incoming parent classes, 59 maximal-deletion roles
and 51 supported forward ideal slots. The complete repair problem is
characterized below, including strict positivity. The eight-record T1
subsystem reduces to six polynomials of degree at most three. Their
vanishing is necessary for repair but not sufficient: an additional
intercept/positivity condition and all fifteen other parent systems remain.
Neither a positive complete repair nor rejection of this 28-child family
is established here.

## 1. Accepted obstruction and unchanged premises

The actual RI109 root adjudication is:

/Volumes/AI_DATA/development/det-review-evidence/ri109-root-concrete-admission-cyl8y1as/RI109_ROOT_FINAL_MATHEMATICAL_ADJUDICATION.json

4,849 bytes; SHA-256
26ddd81d5fc1ae80c30d68aeadcb0ada0e18168bf3892b0995643294e6a64ddf.

It accepts the independently reconstructed native cubic's zero real roots
on (0,R], following a genuine completed audit. Consequently RI103's
specified 27-child repair is rejected under its accepted premises. It does
not reject every extension or this one-column enlargement. RI103's older
"unresolved" statements record the earlier state and are not current
mathematical acceptance.

Retain the conditional scalar-passive law: every individual ideal eligible,
strict positive normalized probabilities, complete binary record cubes,
immutable independent fair newborn bits, strict precursor-record locality,
marked equivariance, and whole maps q_B(P,r,S)D/2. The unmarked parent may be
inspected; a proper precursor may read only its internal records.
The payload D is retained, but not made informative. These remain model
premises, not newly derived DET axioms or general quantum dynamics.

Use RI101/103 notation. T_j (j=1,...,11) are the supported seven-event
classes, z1=1, J_j=T_j disjoint A1; C_i (i=1,...,5) are the affected
six-parent classes, D_i=C_i disjoint A1. Here C_i with an index labels
that five-class catalogue; C3 without an index means the three-chain.
Let F(P) append a new vertex above all of P. The incoming parent set is

\[
 {\cal P}=\{T_1,\ldots,T_{11},D_1,\ldots,D_5\}.
 \tag{1}
\]

The positive fixed seed is h7(T_j)=1+z_j/4 and h7=1 elsewhere; h=1
through six births. Baseline scales rho=a6 and s=a7 are the actual global
half-scales, not adjustable parameters. Their numerical values and M6/M7
are not calculated. The actual rho is rational, with 0<rho<=R, as proved
in RI103. No surviving algebraic root may be substituted for that scale.

## 2. Exhaustive incoming closure after one extra column

Enlarge only the previous support:

\[
 {\cal H}_{28}
 =\{J_j:1\le j\le11\}\cup\{F(P):P\in{\cal P}\}
       \cup\{Y_0\},\qquad Y_0=C_3\oplus A_5.
 \tag{2}
\]

Every child has one finite real correction y_H shared across all labels,
records, newborn bits, ideals and incoming parents. Set y_H=0 on every
other eight-event class and h8(H)=1+y_H/4. The requirement is y_H>-4,
not weak nonnegativity of h8 and not per-record choices.

For completeness the existing J-column deletion inventory is:

| Child | Isolated maximum deletion | Other maximal deletions |
| --- | --- | --- |
| J1 | T1 once | D1 four times |
| J2 | T2 once | D2 twice; D1 once |
| J3 | T3 once | D3 once; D1 once |
| J4 | T4 once | D4 once; D2 twice |
| J5 | T5 once | D2 twice |
| J6 | T6 once | D3 once; D2 once |
| J7 | T7 once | D5 once; D2 once |
| J8 | T8 once | D3 twice |
| J9 | T9 once | D4 three times |
| J10 | T10 once | D5 once; D4 once |
| J11 | T11 once | D5 twice |

There are 38 individual backward roles here, with no orbit division.
Each F(P) has a unique maximum; deleting it gives only P. Thus the sixteen
distinct full children add sixteen roles and no parent classes.
Y0 is connected with five maxima, so it is neither a disconnected J_j
nor a unique-maximal F(P). Each of its five maximal deletions yields
T1=C3 ordinal-sum A4. This adds five roles and no parent classes.
Every possible birth parent comes from deleting a maximal newborn, so
this exhausts incoming parents globally. Totals are 28 columns, sixteen
parent classes and 38+16+5=59 backward roles.

Forward ideal slots have different multiplicities. Each T_j has one
J_j empty-birth slot; the D_i have respectively 7,5,4,4,3 J-producing
slots, inherited from the complete six-parent inventory. These total
11+23=34. Full children contribute sixteen additional slots.
At T1 the unique extra slot yielding Y0 has precursor exactly C3.
It contributes one forward slot, not five: the other four cap vertices
already exist. There are therefore 34+16+1=51 supported forward slots.

No outside parent reaches this support. Outside complete correction
rows remain zero on both sides. Unchanged ideal slots still have their
positive baseline probabilities; only their corrections vanish.

## 3. Record domain and maximal-bit invariance

The complete raw domain is sixteen parents times 128 markings. No new
record is added or discarded by the proposed support change.

The RI103 maximal-record lemma applies to baseline B: every q_B(P,r,S),
including its full complement, ignores the bit on each maximal vertex
of P. Briefly, induct on parent size. If a proper ideal S excludes a
maximum w, use locality. If w belongs to S, a maximal v outside S exists.
Remove v, put T=past(v), and compare the complete diamond for births
with precursors T and S. Its identity is

\[
 q_B(P-v,T)q_B(P,S)
 =q_B(P-v,S)q_B((P-v)+S,T),
 \tag{3}
\]

with all inherited/newborn marks and terminal coordinates transported.
The two T factors ignore w by locality; the other old factor ignores w
by induction because w remains maximal in P-v. Positivity permits
division. The full complement inherits invariance from the sum of the
proper terms. The singleton start follows from record-blind empty birth.
This proof requires complete marked diamonds, not merely permission
to read records or equality at selected zero-mark rows.

At T1, the new core precursor C3 excludes all four cap maxima, so its
probability ignores cap marks directly by locality. The full probability
ignores them by the lemma. All eight assignments xi=0,...,7 to the
three stem vertices must still be retained. No theorem here reduces
them to one bit, two records or an average.

For all parents together the inherited maximal-bit representatives remain
280 connected rows plus104 disconnected rows, totaling384. This is an
analytic quotient of the2048-row quantifier, not a generated table.
Marked equivariance transports every representative equation back to all
labels and the full record cubes.

## 4. Complete 28-column correction system

Define shared variables and positive baseline coefficients

\[
 w_j=e_{T_j}y_{J_j},\quad e_P=q_B(P,r,\varnothing)>0,\quad
 v_j=y_{F(T_j)},\quad d_i=y_{F(D_i)},\quad t=y_{Y_0},
\]
\[
 f_j(r)=q_B(T_j,r,T_j),\qquad
 g_i(r)=q_B(D_i,r,D_i),\qquad
 k_\xi=q_B(T_1,\xi,C_3).
 \tag{4}
\]

Empty probabilities are record-blind. For each D_i use

\[
 L_i(r;w)=
 \sum_{S:\,[C_i+S]=T_j}q_B(C_i,r,S)w_j,
 \tag{5}
\]

counting every individual supported ideal. The empty/ordinary-birth
whole-map diamond gives
q_B(C_i,r,S)e_{C_i+S}=e_{C_i}q_B(D_i,\widetilde r,S),
separately for all records and newborn bits. Consequently the complete
necessary-and-sufficient row equations for this support are

\[
 w_1+k_\xi t+f_1(\xi)v_1=1
        \quad(\xi=0,\ldots,7),
 \tag{6}
\]
\[
 w_j+f_j(r)v_j=z_j\quad(j=2,\ldots,11;\ \text{every }r),
 \tag{7}
\]
\[
 L_i(r;w)+e_{C_i}g_i(r)d_i=0
        \quad(i=1,\ldots,5;\ \text{every }r).
 \tag{8}
\]

The old isolated D_i bit is suppressed only using the proved lemma;
all other required records and ideal multiplicities remain.
Exactly the T1 core slot changes. No other parent equation acquires t.

Strict positivity is exactly

\[
 w_j>-4e_{T_j},\qquad v_j>-4,\qquad d_i>-4,\qquad t>-4.
 \tag{9}
\]

**Proof of equivalence.** Complete normalization of
q_Q=q_B h8(child)/h7(parent) gives these correction equations. Conversely
(6)--(9) normalize every incoming row, while closure discharges every
outside row. They make every multiplier positive. Its dependence only
on unmarked classes preserves strict record locality and marked
equivariance. On each whole-map diamond the intermediate h7 factors
cancel and both paths acquire the same terminal h8, including both
fair-bit factors. This is a through-eight-birth theorem, not all-size
existence.

## 5. The D1 slot remains binding, but v1 need not be positive

Let K=C3 ordinal-sum A3, D1=K disjoint A1 and I=C3.
The accepted RI102 bound is

\[
 {\cal R}_\xi=q_B(K,\xi,I)/e_K>41
 \quad\text{for every stem assignment}.
 \tag{10}
\]

The I birth from D1 still produces J1, not Y0 or a full child.
Its seed denominator h7(D1) is one, and its transformed probability is

\[
 \widehat q(D_1,\xi,I)
 =q_B(D_1,\xi,I)+\tfrac14{\cal R}_\xi w_1.
 \tag{11}
\]

Every positive normalized complete row has this entry strictly below one.
Thus any repair must obey the stronger slot-specific bounds and hence

\[
 w_1<W_\xi:=
 \frac{4[1-q_B(D_1,\xi,I)]}{{\cal R}_\xi}
 <\frac4{41},\qquad w_1<\frac4{41}.
 \tag{12}
\]

The bound uses only the unit row sum, not a half-mass cap on the repaired
proper births. The baseline's half-scale property is not imposed on Q.

Unlike RI103, (6) now gives
f1*v1=1-w1-k_xi*t. The new term can have either signed correction.
Therefore 1-w1>0 does **not** imply v1>0. Reusing that old necessity
would discard allowed cases without proof.

## 6. Exact all-eight-record elimination on held rows

All expressions below are formal in the existing held probabilities.
For each xi=0,...,7 and S in {0,1,3,7}, let

\[
 A_\xi(S)=q_B(C_3,\xi,S),\quad
 B_\xi(S)=q_B(C_4,\xi,S),\quad
 G_\xi(S)=q_B(H_5,\xi,S),\quad H_5=C_3\oplus A_2.
\]

Set a=A(7), b=B(7), g=G(7), c=q_B(C4,xi,15),
h=q_B(H5,xi,15)=q_B(H5,xi,23), j=q_B(H5,xi,31), with xi
understood on each scalar. To avoid confusing a held scalar with v_j,
write

\[
 p_\xi=a_\xi^3g_\xi^6/b_\xi^8>0,\qquad
 \nu_\xi=h_\xi^3/c_\xi^2,
\]
\[
 E_\xi=\sum_{S=0,1,3,7}
           \frac{A_\xi(S)G_\xi(S)^3}{B_\xi(S)^3}
            +3h_\xi^2/c_\xi+3j_\xi,\qquad
 K_\xi=\sum_{S=0,1,3,7}
           \frac{A_\xi(S)^3G_\xi(S)^6}{B_\xi(S)^8}.
 \tag{13}
\]

All denominators are strictly positive. The unchanged RI89/94 deletion
identities, with every repeated factor/ideal counted, give

\[
 U_\xi(x)=4-4xE_\xi+6x^2j_\xi+4x^3\nu_\xi+x^4K_\xi,
\]
\[
 k_\xi=s\rho^4p_\xi,\qquad f_1(\xi)=1-sU_\xi(\rho).
 \tag{14}
\]

The core proper ideal has multiplicity one. The remaining proper ideals
have counts three other stem ideals, four one-cap, six two-cap and four
three-cap ideals. The formula for U includes all eighteen proper ideals,
not only the new supported slot. Unique-top/twin-top transport justifies
zero cap bits in the held C4/H5 rows; it does not erase any stem bit.

Use reference xi=0 and Delta_xi X=X_xi-X_0. Define

\[
 P_\xi(x)=-4\Delta_\xi E+6x\Delta_\xi j
                 +4x^2\Delta_\xi\nu+x^3\Delta_\xi K,\qquad
 \Delta_\xi U(x)=xP_\xi(x).
 \tag{15}
\]

P_1 is precisely RI109's accepted root-free P01. It is nonzero at every
x in (0,R], where R=1/[2(1+max(E_0,E_1))]. This is a cited accepted
result, not a new evaluation. In particular Delta_1 f1=-s*rho*P1(rho)
is nonzero at the actual scales.

Subtract the reference row of (6) and divide only by s*rho>0:

\[
 \rho^3\Delta_\xi p\,t-P_\xi(\rho)v_1=0
       \quad(\xi=1,\ldots,7).
 \tag{16}
\]

If t=0, the pivot xi=1 forces v1=0; (6) then forces w1=1,
contradicting (12). Thus every positive repair requires t nonzero.

Use the justified nonzero pivot P1, never Delta_1 p, to put

\[
 \beta=\frac{\rho^3\Delta_1p}{P_1(\rho)},\quad
 \Gamma=k_0+f_1(0)\beta,\quad
 v_1=\beta t,\quad w_1=1-\Gamma t.
 \tag{17}
\]

The remaining six compatibility conditions are exactly

\[
 M_\xi(\rho)=0\quad(\xi=2,\ldots,7),\qquad
 M_\xi(x)=\Delta_\xi p\,P_1(x)-\Delta_1p\,P_\xi(x).
 \tag{18}
\]

Every M has degree at most three; vanishing leading coefficients are
allowed. No numerical coefficient or actual degree is inferred here.

**Rank theorem.** The eight-by-three matrix with rows (1,k_xi,f1(xi))
has rank at least two because f1(1) differs from f1(0).
If some M_xi(rho) is nonzero, its rank is three and (6) has only
(w1,t,v1)=(1,0,0), rejected by (12).
If all six vanish, the rank is two and its entire affine solution set
is (1,0,0)+t(-Gamma,1,beta). This follows directly from the nonzero
pivot elimination above, proving both directions. Thus two-record
fitting is insufficient: it leaves six remaining conditions to check,
which may themselves be zero or algebraically dependent.

### Zero cases are part of the theorem

- If Delta_1 p=0, then beta=0. All six minors vanish iff every p_xi
  equals p_0, since P1(rho) is nonzero. Then v1=0 and Gamma=k0>0.
  If another p differs, the family is rejected at that actual rho.
- If Gamma=0, every all-record T1 solution still has w1=1. The D1
  bound rejects it despite successful record-rank compatibility.
- If Gamma is negative, (12) forces t negative; then t>-4 is
  consequential. Because k0 and f1(0) are positive, Gamma<0 also
  implies beta<0, giving v1=beta*t>0. Other signs are not prescribed.
- No division by t is used to claim that the t=0 case was feasible.
  It is eliminated separately by the accepted contrast and D1 bound.

## 7. Strict local feasibility and the complete remaining system

After all six minors vanish at actual rho, the T1 rows, positivity of
its three corrected child classes, and the **coarse necessary** D1 bound
w1<4/41 are equivalent to the open interval conditions

\[
 {\cal I}=\{t:\ t>-4,\ \beta t>-4,\
          37/41<\Gamma t<1+4e_{T_1}\}.
 \tag{19}
\]

This is an exact characterization of that stated subsystem only.
The slot-specific W_xi constraints in (12), and complete D1 row equations,
are stronger than the coarse bound and have not been discharged by it.

For clarity put a=37/41 and b=1+4e_T1, so b>a>0.
If Gamma>0, (19) is the interval

\[
 \frac a\Gamma<t<
 \min\left(\frac b\Gamma,\
   \begin{cases}-4/\beta,&\beta<0,\\+\infty,&\beta\ge0.\end{cases}\right).
 \tag{20}
\]

It is nonempty exactly when the displayed upper endpoint is larger than
the lower one. In particular beta=0 gives a positive local interval,
not a full repair. If Gamma<0, necessarily beta<0, and (19) becomes

\[
 \max(-4,b/\Gamma)<t<a/\Gamma.
 \tag{21}
\]

It is nonempty iff Gamma<-37/164. If Gamma=0 it is empty.
These are symbolic inequalities; no sign or value of actual beta/Gamma
has been evaluated. Equality at an endpoint never counts as positivity.

Here is a complete reduction that keeps every other parent obligation.
Choose reference marking r0 for each parent, write f_j0=f_j(r0),
g_i0=g_i(r0), and substitute (17) for w1,v1. For j=2,...,11 require

\[
 (z_j-w_j)[f_j(r)-f_{j0}]=0\quad\text{for every }r,\qquad
 -4e_{T_j}<w_j<z_j+4f_{j0},
 \tag{22}
\]
\[
 v_j=(z_j-w_j)/f_{j0}.
\]

For every i=1,...,5 require

\[
 g_{i0}L_i(r;w)-g_i(r)L_i(r_0;w)=0\quad\text{for every }r,
\]
\[
 L_i(r_0;w)<4e_{C_i}g_{i0},\qquad
 d_i=-L_i(r_0;w)/(e_{C_i}g_{i0}).
 \tag{23}
\]

All denominators are positive. Equations (18), a value t in (19), and
(22),(23), with the shared w1=1-Gamma*t, are **necessary and sufficient
for the complete 28-child repair** at the fixed actual B and amplitude.
For sufficiency recover every full correction as above and apply
(6)--(9). Necessity follows from the reference-row eliminations and
the derived bound (12). Although only the coarse bound appears in
(19), (23) plus complete positivity/normalization restores the stronger
slot bounds automatically. They are not silently omitted from the
complete theorem.

The remaining unknowns may thus be t and w2,...,w11, not independent
per-record full-child values. This exact reduction does not assert
existence of any such tuple. Constant f_j, including the accepted
T8/T11 cases, must retain their allowed freedoms; blanket record
sensitivity is false. Nor does nonconstant g_i alone force d_i=0.

## 8. Smallest justified prospective all-record rank decision

No analytic sign/identity argument in the accepted conclusions settles
the six M polynomials. Neither record-rank feasibility nor a positive
complete family has been proved. The bounded remaining prerequisite
requires only the already held domain:

| Held order | Complete record rows | Slots per row | Total slots |
| --- | ---: | ---: | ---: |
| C3=(0,1,3) | xi=0,...,7 | 4 | 32 |
| C4=(0,1,3,7), top mark zero | xi=0,...,7 | 5 | 40 |
| H5=(0,1,3,7,7), both top marks zero | xi=0,...,7 | 7 | 56 |
| Total | 24 | | 128 |

These are a fixed subset of RI88's accepted40-row/224-slot held data.
Full rows, positive denominators, normalization, transport and equal twin
slots remain in the input contract. The held T1 profiles p,E,U
use no C5 row, new parent shape, H/z calculation beyond accepted z1=1,
new q6/q7 table or actual scale evaluation.

The smallest record count for a nontrivial rank minor is three: reference0,
the accepted nonzero-contrast pivot1, and another xi. Those three records
use nine complete held rows/48 slots in this protocol. A proven nonzero
minor at actual rho suffices to reject. But one selected triple does not
test all records. The fixed complete reduction needs the six minors
xi=2,...,7, using the24 rows above; it needs neither all21 pairwise
contrast minors nor a new eight-event enumeration. These are justified
domain/count reductions, not an assertion of information-theoretic
minimality or authority to run an adaptive search.

A future separately reviewed exact decision may construct (13),(15),(18)
as rational polynomials without choosing rho or s. It must distinguish:

1. All six minors identically zero: the rank condition holds throughout
   the outer interval; no fictitious nonzero gcd may be assigned.
   Intercept, strict positivity and remaining parent equations stay open.
2. At least one nonzero minor: omit only proved identically zero members
   and form the monic rational gcd G of the nonzero minors. At any
   x in (0,R], all minors vanish iff G(x)=0. A nonzero constant gcd
   excludes every candidate. Otherwise deg(G)<=3.
3. No real root of G on (0,R], or a complete proof of no rational root
   there: sufficient rejection of this exact support, because actual
   rho is rational. Both zero-polynomial and degree-drop cases must
   be handled exactly; rho=0 is excluded and rho=R is included.
4. A surviving admissible common root: unresolved, not actual rho,
   not a selected new baseline, not positivity and not a complete repair.
   A zero of one minor alone is weaker still.

The unchanged outer interval need not enforce every other marked-row
baseline constraint at arbitrary trial x. This does not weaken root
exclusion on that larger interval; it further limits what a surviving
root can mean. No numerical s can be selected to make Gamma favorable.

This design is a necessary all-record algebraic decision, not a solver
for (22),(23). A later full feasibility decision would need its own
explicit domain and admission. Nothing here authorizes that enlargement.

## 9. Handoff and claim limits

New results are the complete one-column closure/equations, exact
nonzero-pivot all-record elimination, rank/zero-intercept distinctions,
strict local interval classification, and necessary-and-sufficient
remaining shared-parent system. The actual 28-child feasibility remains
unresolved; the previously accepted27-child rejection remains intact.

Only source reading, manual symbolic proof and opaque evidence identity
checks are used. No target/helper import, compilation, AST evaluation,
solver, fixture/certificate campaign, native coefficient evaluation,
H/z arithmetic or actual rho/s/M6/M7 choice occurs. All original evidence
and repository files remain unchanged.

This is an external manuscript for prospective
docs/track_b/native_growth_one_column_repair_v1/REPAIR.md.
Premise/source/review metadata are external, not executors.
Root retains acceptance, repository/index/git/push and every future active
admission. RET remains paused; the measurement lane proceeds separately.
No informative full QM derivation, all-size extension, empirical
correspondence, native geometry or gravity follows. This handoff ends the
worker's bounded assignment, not the ongoing programme.
