# RI98 — exact direct trace aggregation, source-only packet

This packet implements the accepted [RI97 proof/design](../gwosc_mode_weighted_trace_v1/DESIGN.md)
in a fresh external evidence directory. The relative link targets the intended
publication location `docs/experiments/gwosc_mode_weighted_trace_check_v1/`;
it is prospective in this external snapshot. The exact accepted design is
23,571 bytes, SHA256
`65edcabef43e15cfa44299c19276c194aed717269fe17702877f76ca8af379e7`.
Its root adjudication is 2,430 bytes,
`a2b76efbe5105c286c8b50d9183e407726c3404b36309d0ed9fb65c95b1b9539`.

**No target import, executable compilation, fabricated qualification,
actual numerical operand decoding, new transform or execution has occurred.**
Source/AST and byte-metadata work is the entire authoring scope. This note
is neither a completed result nor an execution authorization. The independently
authored validator and complete source reviews must be accepted before root
can freeze any future qualification call.

## Scientific operation and evidence

`consumer.py` has no I/O on import, CLI, local source import, scientific package
dependency, FFT or coefficient reconstruction. `run_saved(body, provenance)`
binds the complete RI96 result bytes before JSON decoding and projects only
the already accepted alpha values, midpoint Q256 rectangles, four complete
source PSD records, band extrema, C-minus/C-plus/error margins and scalar trace
bounds. It retains the full input byte identity and rechecks it after the
fixed operation. Actual full upstream custody and genuine transform validation
are explicit inherited premises, not conclusions inferred from hashes.

The projection is a new exact selection of accepted fields, not a re-encoded
replacement for the admitted full operand. No RI73 interval body or RI90/83
numerical body is opened by this new operation. Unselected RI96 fields remain
authenticated by the full held body and its accepted predecessor review.
Closed projection/result fields are specified completely in `API.md`.

The consumer computes exactly 8192 scalar mode records. For each row/component,
it expands `2*abs(center)*tau+tau²` and
`2*abs(center)*alpha+2*tau*alpha+alpha²`, then applies the pair multiplicity.
Nyquist omits both imaginary midpoint width and imaginary alpha contribution.
DC is retained in input identity and endpoint compatibility, but its true
zero action contributes no scalar term. Midpoint DC is never projected out
of an inherited Parseval record. No new Parseval or transform is computed.

The four scalar scenarios use every original binary64 PSD bin as its exact
dyadic rational, with fs4096 endpoint/interior normalization unchanged. Each
independently checks the fixed-band extrema and all tied indices, reconstructs
the retained raw band trace and its exact spread/margin decomposition, checks
the direct-upper consistency theorem, and forms only the scalar intersection
with the accepted RI96 trace intersection. It preserves negative raw bounds,
zero-width intervals and fixed null/undefined-reason records. There is no
bin selection, floor, floating tolerance, minimum improvement or refitting.

The inherited262144-bit convention checks decoded/explicit integers and the
numerator/denominator of each guarded completed Fraction operation. It is not
a guarantee about every temporary product or allocation inside Python. All
existing checked-operation boundaries remain in place; no cross-cancellation
rewrite or new arithmetic/rejection domain is introduced for this packet.

The separately authored `validate.py` uses the product form
`(|center|+tau+alpha)²-center²`, independently computes the transform-only
product difference, and subtracts to obtain the alpha contribution. It
reconstructs every scalar record from independently decoded admitted fields,
without importing or calling the consumer. The same arithmetic serves the
ten non-production analytic cases and the full production-shaped fabricated
case. The validator inherits the already completed Fourier/row enclosure
certificate explicitly; it does not claim another independent FFT.

## Prospective qualification inventory

`qualify.py` accepts already captured primary and independent-validator module
objects. It imports neither implementation. Its only file operations retain
three new regular files in a caller-provided canonical, empty artifact directory.
It has no original-input path, HDF parser or observed-data acquisition step.
The sole call to actual-entry projection is a refusal using the bytes `{}`;
the immutable fixed input gate must reject those bytes before scientific parse.

There are exactly eleven named positive cases, Q1–Q11, as accepted in RI97.
Q1–Q5 use the two exact M4 rows with modal powers2 and2 and check colored,
white-eigenvalue, interior-only, Nyquist-only and DC-only outcomes. Q1 also
changes only the retained PSD DC value; all derived scalars must remain equal.
Q6/Q7 use the correlated interval row and explicitly check the weighted error,
transform/alpha split, negative raw lower bound and unchanged prior intersection.
Q8 isolates within-band spread, Q9 isolates the row-sum inflation of matrix
margins, Q10 traverses every production-shaped zero row/mode/scenario, and
Q11 checks exact quadratic scaling and unchanged ratio statuses. No FFT runs.

The tiny kernel domains are precisely (M,row-count)=(4,1),(4,2),(8,1),(8,8),
with fs4096. Their M4 divisor2/alpha definitions are analytic fixture facts.
The real `build_result` has no size parameter and requires M16384/eight rows.
Q8/Q9 are periodic response-algebra fixtures, not admissions of the fixed crop.
Q10 is an explicitly fabricated zero operator, not a claimed RI73 positive-Gram
certificate or a predecessor execution receipt.

Every tiny positive first runs the independent validator's complete
`validate_case`, then compares selected quantities to hand-derived exact
expectations. The coherent held band matrices are constructed separately by
the qualifier from the explicit RI92 entrywise bound; they are not generated
by the consumer. Those construction formulas are not presented as the sole
independent oracle. Full production zero-result validation, complete canonical
serialization, fresh decode and full independent reload are also required.

The literal refusal inventory has **59 cases: 39 producer and 20 validator**.
Each must raise the named exception class and exact intended `.code`; accepting
an operand, or rejecting for a different reason, fails the qualification. The
closed inventory is in `qualify.REFUSALS`, which can be extracted by AST without
importing the source. Its domains are:

- Whole actual body pin, duplicate/nonfinite JSON, canonical rational/hex and
  integer bit ceiling, unsupported dimensions and complete row/mode inventory.
- Reversed or incompatible endpoint rectangles, negative alpha, PSD size/type/
  complete byte identity, negative/nonfinite PSD and conjugate/Nyquist weights.
- Exact extrema/ties, spectral-spread identity, nonnegative matrix margins,
  ordered priors and empty intersection.
- Fixed design/input identity, actual-versus-fabricated context, false claimed
  completed qualification/custody and exact scenario order.
- Full result key/phase/method/provenance/limitations/check inventories,
  mode scalar errors, missing lambda weighting, imaginary Nyquist alpha,
  alpha incorrectly counted as transform-only, clipped raw bounds, altered band
  width/final interval/source identity, false finite undefined ratios and
  noncanonical serialized output.

Late result mutations target closed guards that the full validator actually
calls. Expected records are first independently reconstructed/checked. This
avoids rerunning complete production arithmetic for every one-field schema
mutation; it is not cached admission, skipped positive validation or a patched
predicate. Input/provenance mutation slots are restored in `finally` and their
whole canonical identities are checked before artifacts are written.

Exactly three canonical regular files are retained:

| Artifact | Content |
|---|---|
| `ANALYTIC_CASES.json` | Complete tiny inputs, held band premises, mode terms, outputs and exact anchors; Q10 references complete retained files. |
| `FABRICATED_PROJECTION.json` | Entire production-shaped zero projection with explicit fake context and no predecessor-execution claim. |
| `FABRICATED_RESULT.json` | Entire production-shaped scientific-format fabricated output, validated and decoded again independently. |

The compact qualification return has closed keys schema,phase,status,
provenance,counts,positive_cases,refusal_inventory,refusals,artifacts,scope,
limitations. schema is `ri98-fabricated-qualification-v1`, phase is
`fabricated_qualification`, status is `all_declared_checks_passed`. Counts are
11 analytic cases,8 production rows,8192 retained non-DC terms,4 scenarios,
59 refusals and3 artifacts. All booleans and integer counts remain typed.
Artifact identities are relative names/bytes/SHA256; paths, timestamps and
inodes belong to separate prospective caller custody, not scientific stdout.
No `QUALIFICATION.json` exists or is fabricated during authoring.

## Required later source/caller admission

The future caller is not implemented or authorized by this packet. It must
capture the exact four reviewed scientific files plus this API and the accepted
design, source-review records and required predecessor acceptance records.
Imports must use the captured admitted module bytes, not ambient local import
resolution. The source/implementation pin in `provenance.sources` binds this
note; API and review records remain part of the full external source closure.

The external caller must verify the genuine recovered runtime/interpreter chain,
full qualified source/runtime inventory and exact isolated environment before
loading target modules. A version string alone is insufficient. There is no
runtime download, package change, newly reduced fingerprint or platform rewrite.
The prior RI96 genuine runtime and accepted supervision remain the reference
for a separately reviewed minimal caller adaptation, if root later assigns it.

The prospective qualification entry is exactly
`qualify.run(primary, validator, provenance_template, artifact_dir)`.
The caller supplies genuine captured-source/runtime identities, accepted RI97
and RI96 root metadata, input replaced with the actual generated fake projection
pin, and null qualification/custody acceptance. Qualification success cannot
be represented by a hand-authored receipt or a purely source-level assertion.

Future qualification runs are serial: normal first, genuine completion and
complete evidence review, then separately admitted optimized execution. Both
must retain byte-identical complete qualification outputs and all three artifact
bodies. The intended interpreter flags remain isolated `-I -B`, with `-O`
only in the second mode; literal interpreter paths, argv, cwd, environment,
source copies, exclusive output destinations and custody records require a
new concrete root-reviewed freeze. This note creates none of those objects.

The resource envelope remains exactly 180 active-child seconds, 524288 KiB
sampled sole-child RSS, target polling0.025 seconds, maximum/final successful
sample gaps0.1 seconds and per-ps timeout0.05 seconds. The later supervisor
must preserve raw attempts, parsed samples, observed peaks, genuine child and
outer exits, before/after identities and output custody. This is sampled child
RSS, not an OS hard cap or process-group sum. No success under that envelope is
inferred from source reading. A failure must be preserved; it allows no bin
omission, reduced independent validation or relaxed gate.

Only after accepted actual qualification may root separately consider the fixed
saved application. Its sole numerical operand is the complete RI96 result,
41,673,703 bytes/SHA256
`5d0af7febecefd4db07bd93165b2fde7d887e7d3e7caf0243961638538db6580`.
Full normal/optimized RI96 input/output/runtime/transform/custody admission,
its independent saved arithmetic report and root acceptance are mandatory
premises as specified in RI97. A new actual execution and complete independent
saved-result validation require their own reviewed binding and authorization.

All scopes remain distinct: mathematical enclosure, fabricated software
qualification, inherited custody, actual fixed saved application, empirical
noise adequacy and calibrated physical conclusions. No current field promotes
the already used32-second data to held-out evidence. No native DET forward
map, physical gravity conclusion, SNR, p-value or RET work is introduced.

## Current source status

Initial source authoring is complete for consumer/qualifier/API/protocol. The
independent validator is written and its reservation released. Complete peer
source reviews passed; their identities and final source pins are retained in
the source-only handoff. Root source acceptance and all execution remain
pending. No source file in this external packet has been imported or executed.
